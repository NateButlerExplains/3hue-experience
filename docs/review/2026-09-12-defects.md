# Tour script v2 — verified defects, by chapter

_Each chapter of the draft script was checked against content/builder-names.json, content/experience.json, content/surfaces.json and tools/check-manifest.js by an independent reviewer instructed to find faults. 2026-09-12._

## Arrival  
`arrival` · verdict **defects** · 16 findings

| # | severity | category | line | problem | fix |
|---|---|---|---|---|---|
| 1 | blocker | guides | `arrive-q1 / arrive-q2 / arrive-q3` | `who: lead` is not a guide id. I patched the chapter into content/tour.json in memory exactly as written and ran `lintTour` from tools/check-manifest.js: it returns 3 errors, not 0 — "nodes.arrive-question.lines[0].who: who must name a guide: avi, huey" and the same for lines[1] and lines[2]. The draft's headline verification claim ("0 errors, 1 pre-existing warning") is false for the chapter as written. Baseline on the untouched file really is 0 errors / 1 warning, so the three errors are introduced by this chapter. | Drop the `who` key entirely on arrive-q1/q2/q3. An unpinned line is spoken by the lead, which is the intent; `who` only ever takes `avi` or `huey`. Re-run the lint after the change — with `who` removed the patched file does return 0 errors. |
| 2 | blocker | graph | `arrive-question — Ask suggests` | The draft cuts `where-to-start` from `arrive` with the stated reason that it "carries `goto: path`, which would teleport a visitor to the tower before they have seen anything" — then suggests it again one node later on `arrive-question`, where the visitor has still seen nothing. I confirmed `where-to-start` does carry `goto: "path"` and that Ask gotos are live reachability roots in the lint. The rationale and the sheet contradict each other, and the teleport the draft says it removed is still on screen at the moment of the sort. | Remove `where-to-start` from `arrive-question`'s ask list. `packages`, `frameworks` or `timeline` would fit the sort moment without a goto; if none fits, three suggestions are legal. |
| 3 | blocker | shelf | `arrive-question choice → gc / wt / sr` | Each option now drops the visitor straight into a door node whose first line still carries the old buyer framing the draft says F retires. `gc-1` reads "This room is for sponsors and family offices carrying cyber risk across companies with no common standard" — a paraphrase-as-label of "Portfolio owners", the exact framing the draft's own reasoning says "would re-break it". Same class in `wt-1` ("This room is for software teams…") and `sr-1` ("This room is for banks, credit unions, lenders and operators…"). A single-company SaaS team that picks the data/vendors/access answer is told in the next breath that the room is for family offices. None of the three lines appears in the "Contract for the other chapters". | Add wt-1, gc-1 and sr-1 to the contract as must-rebuild, and say what they become. Since the visitor has just told you what is being asked, the room-opening line should confirm the ask ("Then this is the room where data, vendors and access get a single owner"), not re-describe a company type. |
| 4 | blocker | graph | `baseline-3` | `start-baseline` line `baseline-3` is gated on `when: {answers: {segment: ["all"]}}` — the only line written for the everything walk. `segment` is stored only by the lobby's `segment` choice, which the new routing bypasses entirely; everything-walkers now store `situation: early` instead. So baseline-3 goes permanently silent the moment this chapter lands, and becomes a hard lint error ("answers.segment: no choice remembers segment") the moment the contract's "CUT the segment choice" instruction is carried out. Neither consequence is mentioned; the contract says only "Nothing in arrival references `segment` any more", which is true and beside the point. | Add to the contract: `baseline-3`'s `when` must be rewritten to `{answers: {situation: ["early"]}}` in the same commit that cuts the `segment` choice, or the everything path loses its baseline line. |
| 5 | major | graph | `arrive-question choice` | Nothing anywhere reads `situation`. The draft says door and path lines "can now also read against `situation`" but supplies no condition that does, while the change silently kills the one condition that was doing this job (baseline-3). The sort's answer is captured and never spent, so the single question the whole chapter is built around currently changes nothing downstream except which node you land on. | Either name the specific lines that will read `situation` (with their ids) so the owner can see the payoff, or state plainly that `situation` is routing-only in this chapter and that the downstream reads land with the door chapters. |
| 6 | major | claims | `arrive-2 — Changed from today` | docs/COPY-GATE.md says the opposite. The tour's sign-off row is "pending", and the rule is explicit: "The OK names the sheet's line hashes. A changed word changes its hash, so a later edit needs a fresh sheet and OK." `approved-copy` is a provenance status on the wording, not an approval of this line in this position. Telling the owner a line in a sign-off sheet needs no sign-off is the one sentence in this document that must not be wrong. | Strike the clause. Replace with: the wording is approved copy from The Message Stack §09, but its placement as the tour's second line is new and rides on this sheet's OK like every other changed line. |
| 7 | major | completeness | `Contract for the other chapters` | Eight ids carry new text, not two. Against the real file: arrive-1 (was "Welcome to the 3HUE lobby. I'm {guide:avi}, one of your two guides."), arrive-2, arrive-4, arrive-5, handoff-avi-1, handoff-avi-2, handoff-huey-1 and handoff-huey-2 all change. Only arrive-3 is untouched. Under the copy-gate hash rule every one of the eight needs a fresh hash and a re-render, so an under-count here is exactly the kind of thing that orphans audio later. | List all eight ids. The "no rendered audio is orphaned" reasoning is sound — media/voice/ does not exist at all, I checked — but the count has to be right for the next person who reads this row. |
| 8 | major | dead-labels | `arrive-question — Changed from today` | All three dead labels are typed verbatim in the sign-off document, and "Portfolio owners" appears a second time in the Reasoning section ("am I a portfolio owner?"). The rule is that these strings appear nowhere. A sheet that goes to the owner is exactly where a retired label gets copied back out of. | Refer to them as `{door:*.icp}` / "the three buyer labels" without printing the strings. The point being made — that they asked the visitor to classify their company — survives without quoting them. |
| 9 | major | claims | `Reasoning §1 and arrive-1 rationale` | Three unsourced figures in the owner-facing rationale. "all nine verified prospects" — there is no research artefact anywhere in the repo supporting a count of nine prospects; `sources` lists nine entries, which may be where the number came from, and if so it is a category error being presented as evidence. "Win Trust's 28 services" — the arithmetic happens to check out (10 + 14 + 4 from builder-names.json) but no source is cited, and no manifest field carries it. "stay for the next ninety seconds" — "ninety" is a figure under the repo's own `figures()` regex. The rationale is the part the owner reads to decide; unsourced numbers there carry the same weight as unsourced numbers in a line. | Cut all three. "Every prospect in the research had the same thing happening", "Win Trust's three service families", and "what makes a visitor stay" say the same things without a number that cannot be cited. |
| 10 | major | names | `Reasoning §1` | Three Builder names are typed as plain text: "Privacy Leadership Launch", "Incident Response Fast Start" and "MXDR Complete Protection". All three are `offers` entries in experience.json (`privacy-leadership-launch`, `ir-fast-start`, `mxdr-complete`) and package names in builder-names.json, and all three are in the set `builderTypedNames()` guards — typed into a line they would be a hard error ("typed Builder name…; use a token"). Category names (Risk Assessment & Risk Management, etc.) are not guarded and are fine as prose; the three package names are not. | Write them as `{offer:privacy-leadership-launch}`, `{offer:ir-fast-start}`, `{offer:mxdr-complete}` even in the rationale, so the sheet models the rule it is asking the owner to sign off on. |
| 11 | major | vocabulary | `arrive-question option `run`` | "watching" is the inflected form of forbidden term "watched" and makes the same continuous-monitoring claim. The lint's substring test misses it ("watching" does not contain "watched"), so this passes by accident, not by design. Worse, it is 3HUE's MDR pitch put into the visitor's mouth: the button is supposed to say what the counterparty is asking for, and "who's watching in between" is an unsourced service claim with no `source` behind it. Also the longest, hardest button on the page — 16 words, two clauses. | "They want to know what happens when something breaks, and who owns it the rest of the time." Drops the forbidden stem, drops the monitoring claim, and reads as the buyer's question rather than the seller's answer. |
| 12 | major | graph | `Reasoning §2` | `lobby-again` is a different node from `lobby` and does not make `lobby` reachable. I checked every edge: the only two pointers at `lobby` in the whole file are `handoff-avi.next` and `handoff-huey.next`, and this chapter repoints both. Door exits (`wt-next`, `gc-next`, `sr-next`, `close`) go to `lobby-again`, never to `lobby`. The lint does stay silent, but only because chapter entries are seeded as reachability roots — so `lobby` survives on the map alone, reachable by no path a visitor can walk. The parenthetical invites the owner to read lint silence as proof of a route that does not exist. | Say it straight: after this change `lobby` is reachable only from the chapter map, no node leads to it, and the lint passes because chapter entries count as roots. Then let the owner decide whether a node no visitor can walk to should keep a chapter. |
| 13 | minor | ear-test | `arrive-2` | 27 words in a single sentence with a suspended em-dash clause, placed as the second thing a visitor hears. It is under the lint's 35-word warn threshold but well over what a listener holds — "security, compliance and AI governance programs" is already a three-item stack before the relative clause and the dash arrive. It reads as written boilerplate because it is written boilerplate. | If the wording is fixed by the owner's instruction, at least mark it for the voice render as two breaths at the dash. If it can be split, the natural break is after "work." |
| 14 | minor | completeness | `arrive-1 / handoff-huey-1 / handoff-huey-2 — Changed from today` | The "was" quotes print guide names as plain text where the real file uses tokens. Today's file has "I'm {guide:avi}, one of your two guides.", "{guide:huey} is all yours…" and "Thanks, {guide:avi}. Let's find your door." Rendered this way the diff reads as if the current script types guide names — which would be a lint error — so the sheet misstates the starting point on the one rule (names only as tokens) it is enforcing elsewhere. | Quote the "was" lines with their tokens intact, and keep the plain-name rendering only in the clearly-marked "Filled, as the visitor hears it" lines, where it belongs. |
| 15 | minor | completeness | `arrive / handoff-* — Ask suggests` | `guides` is kept on `arrive`, but its first answer line `ask-guides-1` reads "…and you chose who leads." At `arrive` the visitor has not chosen yet, so the answer states something false at the only place in the tour where it is guaranteed false. Pre-existing, but this chapter reviews arrive's whole ask list and moves `guides` to first position without touching it. | Add to the contract: `ask-guides-1` needs a `when` on `{answers: {lead: [...]}}` with a pre-choice variant, or the clause dropped. |
| 16 | minor | completeness | `arrive-question option `early`` | Two small inaccuracies in one sentence. `all-1` is "One stop in each room, then where to start." — with a terminal period; the string the draft uses is the existing lobby `all` option's `sub`, which is where it actually comes from. Separately, the merge rationale ("the two audiences want the same thing anyway") sits against the `early` Ask answer this same node suggests: "you may be early, and 3HUE would rather tell you that than sell you something." The button routes the not-yet visitor into the full three-room walk while the Ask answer beside it tells them to wait. | Attribute the sub to the lobby option it is copied from. And either address the tension with `ask-early-1` in the rationale or give option four a sub that does not promise a full tour to someone the manifest says may be too early. |

## Lobby  
`lobby` · verdict **defects** · 20 findings

| # | severity | category | line | problem | fix |
|---|---|---|---|---|---|
| 1 | blocker | graph | `kiosk / option `doors`` | The visitor who reaches the kiosk before walking any room is stranded there. js/tour.js:419-420 drops any option whose target is the entry node of the chapter the visitor is currently in: `const entryOf = target ? T.chapters.find((x) => x.entry === target) : null; if (entryOf && here && entryOf.id === here.id) continue;`. The draft files kiosk in chapter `lobby`, whose entry IS `lobby`. So when the last branch resolves to `lobby` (no door visited), the whole "Back to the doors" option is removed from the rendered choice, leaving only `ask`, which is an action with no `next`. That is exactly the primary route into the node: the draft adds `aivric` to the lobby's ask list, and §D2 wires ask.questions[aivric].goto to the kiosk. The lint does not catch this — a choice with one surviving option is legal — so it ships as a silent dead end. | Either give the kiosk its own chapter (the `optional: true` / `landmark: "kiosk"` alternative the draft rejects), or make the last branch go to a node that is not a chapter entry. Simplest: add a second lobby node (or reuse `lobby-again` unconditionally with a no-door-visited line, see the next finding) and drop the branch list entirely. |
| 2 | major | completeness | `lobby-again` | There is no line for a visitor at `lobby-again` who has walked no door chapter, and that state is reachable. js/tourmap.js:149 jumps to any chapter entry with no visited gate (`api.jump(c.entry)`), so a visitor can go straight from the lobby to chapter `path` or chapter `close`; `close`'s option `another` targets `lobby-again`. With visited = [], again-after-wt/gc/sr, again-two and again-all all fail their `when`, so the visitor hears only again-1 ("Back in the lobby. Opening a second door isn't a correction.") — a correction line for someone who has made no first choice. | Add an unconditioned or notVisited-all fallback line to lobby-again, and drop the "every visitor hears two lines" claim or restate it as a covered-cases table. |
| 3 | major | claims | `again-all` | This is false for every visitor who can hear it in the most common route. `close` is reachable only through the path chapter (start-baseline, start-review, start-running and path-all all have next: "close"), and `close` is the node whose `another` option leads to lobby-again. So a visitor arriving at lobby-again from close has already walked the tower. Telling them "the tower is what's left" contradicts what they just did, and the suggested `start` option sends them back through a chapter that will carry the Visited tag. again-two has the same problem in its second sentence ("Or take the tower, and see where you'd start"). | Add notVisited: ["path"] to again-all and again-two and write a companion pair for the tower-already-walked case (e.g. "All three walked, and you've seen where you'd start" plus an option back to the summary), or route close's `another` somewhere that distinguishes the two. |
| 4 | major | completeness | `lobby` | Two counting errors, one of which hides a gap. (a) For situation = asked/spread/running the visitor hears lines 1-5, one of 6-8, AND lobby-6 — seven lines, not six. Only the `looking` visitor hears six. (b) If `situation` is unset the visitor hears five lines: no pointer and no overrule. That is reachable — the Tour map lets a visitor jump to chapter `lobby` (entry `lobby`) before answering arrival, and a restored sessionStorage state can carry a node without the answer (js/tour.js:338 restores answers and visited independently). The chapter's whole point, "shows them the room their answer points to", silently disappears with no fallback line. | State the real per-branch counts, and add an unconditioned fallback for situation-unset — either a neutral pointer line or make lobby-yours-open the default by conditioning the other three and leaving it uncoditioned last. |
| 5 | major | surfaces | `lobby-yours-open` | For the `looking` visitor the pointer never fires, so the note describes a bug the draft does not fix. st.view.cueDoor persists across lines within a node (js/tour.js:470 sets it, and it is only cleared when a new scene renders, js/tour.js:457), and there is no cue kind that clears a door — checkCue accepts only {station}, {stage}, {door}, {surface}. So the visitor who is told "no door is more yours than another" hears it while the Stay Ready doorway is visibly aria-current in the plate, and it stays lit through the choice. That is the exact opposite of the line's meaning. | Reorder so the always-on door cues do not end on stay-ready, or give lobby-yours-open a cue back to a neutral state, or move the three door cues onto the four lobby-yours-* lines only so nothing is marked for the `looking` visitor. |
| 6 | major | scene | `kiosk` | The node has no `quiet: true`, so arriving from any other chapter raises the wrong chapter title card. js/tour.js:382 fires the card on `chapterChanged && !node.quiet` with no check on whether the chapter was already visited. The Ask panel lets a visitor open any question from any node (js/ask.js `all()` lists all 34), so "What is AiVRIC?" answered inside a door chapter or at close offers "Take me there" and drops the visitor onto the kiosk behind a card reading "Three doors, one path" — the legend for the doors, not the kiosk. The draft explicitly notes `quiet: true` on lobby-again for exactly this reason and omits it here. | Add quiet: true to the kiosk node, or file the kiosk in its own chapter with landmark: "kiosk" so the card that fires is the right one. |
| 7 | major | dead-labels | `§A3 and §D5` | All three retired labels are typed verbatim in the draft — twice in §A3 (once more as "'Portfolio owners' is a phrase nobody says about themselves" in the Changed-from-today block) and again in §D5. The brief is absolute: these strings must not appear anywhere in any casing. §D5 also proposes adding them to vocabulary.retired, which would make the draft document itself the thing the new rule forbids; the retired check in check-manifest.js:596 is a case-sensitive substring test, so any copy edit that lifts a phrase out of this document into a manifest field trips it. | Refer to them indirectly — "the three buyer labels currently on doors[].icp" — and cite check-manifest.js:185 and the manifest paths rather than reprinting the strings. |
| 8 | minor | accuracy | `§A3` | The count is wrong. content/tour.json nodes.lobby contains six `{door:*.icp}` tokens — one each in lobby-2, lobby-3, lobby-4 and one each in the three door option labels — plus three in lobby-again. Nine, not seven. "Every option label was {door:X.icp}" is also wrong: the fourth lobby option is labelled "Show me everything". An owner checking the claim against the file will find it does not hold, which undermines the rest of the audit. | Six in lobby, three in lobby-again, nine cut; three of four lobby option labels. |
| 9 | minor | names | `§C lobby Sources, substantiation paragraph` | Seven Builder names are typed out as plain text rather than tokens: the four above plus Privacy Leadership Launch, Incident Response Fast Start and MXDR Complete Protection. All seven are in builder-names.json packages and all seven are in the set builderTypedNames() feeds to the typed-name check (tools/check-manifest.js:492-501, 573), so any of these strings lifted into a manifest field is a hard lint error. "PCI‑DSS Readiness" is additionally mistyped with a U+2011 non-breaking hyphen where the Builder name has a plain hyphen, so it would not even match its own token. | Write {offer:soc-2-readiness}, {offer:iso-27001-readiness}, {offer:iso-27701-readiness}, {offer:pci-dss-readiness}, {offer:privacy-leadership-launch}, {offer:ir-fast-start}, {offer:mxdr-complete} — all seven ids exist in experience.json offers. |
| 10 | minor | completeness | `lobby-5` | The line promises two destinations and the chapter wires only one. §D2 adds ask.questions[aivric].goto to reach the kiosk; nothing gives the visitor a route to the round table. The round table today is the `close` chapter (chapters[6].title "The round table", entry `close`), reachable only after the path chapter or by a Tour-map jump — so a visitor who hears lobby-5 and wants the people has nowhere to go from the lobby. | Either add a companion change routing an ask question (or a lobby option) to the round table, or cut the round table from lobby-5 and let the close chapter introduce itself. |
| 11 | minor | claims | `kiosk-5` | This truncates the standing sentence and drops its substance. path-price in content/tour.json reads "There's no list price here. Pricing follows scope and commitment, and the team scopes it with you." (source: 3HUE, Inside 3HUE tour, Ask; status: adapted), and ask-price-1 carries the same "Pricing follows scope and commitment" clause. The draft cites the identical source but keeps only the framing half, so the tour now says the price sentence two different ways and the kiosk version asserts less than its source supports. | Use the full sentence, or give kiosk-5 the same text as path-price so the two cannot drift. |
| 12 | minor | completeness | `§C, Changed from today` | Two silent renames are not listed among the changes. The lobby choice id today is `segment` (the draft makes it `lobby-door`) and lobby-again's is `another` (the draft makes it `lobby-again`). Neither is load-bearing at runtime — js/tour.js keys state off choice.remember, not choice.id — but tests/tour-manifest.spec.mjs:316 and :338 both locate the door node by `n.choice?.remember === 'segment'`, and an owner reading the change list would not know the ids moved. Using `lobby-again` as both a node id and a choice id is also needlessly confusing. | List both renames explicitly, and consider leaving the ids alone since nothing depends on them. |
| 13 | minor | guides | `§A6` | Wrong notation for a pin. `who` takes a bare guide id checked against guideIds(m) (tools/check-manifest.js:761: `if (line.who !== undefined && !(gIds.includes(line.who)))`); `{guide:huey}` is a text token, not a valid `who` value, and would fail the lint. The draft's own reasoning also concedes the pin only works when Avi leads — with guide.lead defaulting to "avi" but the visitor choosing at `arrive` (choice `lead`, options huey and avi), a Huey-led visitor would hear the overrule from the lead they already trust, which is the failure mode the pin is supposed to avoid. | Write it as who: "huey", and if the owner takes the option, gate it with when: {answers: {lead: ["avi"]}} the way close-back-huey and close-back-avi already do. |
| 14 | minor | accuracy | `§C lobby, choice notes` | "One stop in each room, then where to start" is 42 characters, not 41. Trivial in itself, but it is one of the few numbers in the document an owner would spot-check, and it is wrong. | 42. |
| 15 | minor | accuracy | `§D4` | sr-1 does not name the buyer labels. Its text is "This room is for banks, credit unions, lenders and operators with governance on paper and an examiner in the building." — an industry list, not the retired ICP string. wt-1 says "software teams" and gc-1 "sponsors and family offices". The recommendation to cut all three may still be right, but the stated justification overstates what is in the file. | Say all three re-sort the visitor in prose; none reprints a buyer label. |
| 16 | minor | accuracy | `§C lobby-again, Changed from today` | Both halves overstate today's state. The Tour map lists chapter `path` as a row titled with strings.tourMapTower and jumps to it unconditionally (js/tourmap.js:120-127, :149), so the tower is reachable from anywhere. And today's lobby-again has no hideWhen, so an all-three-walked visitor still sees three options (tagged Visited) rather than nothing. The new fourth option is a genuine improvement; the case for it does not need the exaggeration. | "Today the tower is reachable from a room or the Tour map, but not from lobby-again's own choice; with hideWhen added, the fourth option is what stops the choice emptying out." |
| 17 | minor | ear-test | `again-after-wt / again-after-sr` | "the running" and "everything you run" collide inside one sentence in two different senses — the noun for the Stay Ready shelf and the verb for the business the visitor operates. again-after-wt has the mirror problem ("everything you run, and running it day to day"). Spoken aloud, a listener hears "run" three times in nineteen words and loses which one names the door. | Vary one side: "You've done the day-to-day. What's left is proving it to whoever asks, and owning it across every company and system you carry." |
| 18 | minor | ear-test | `kiosk-4` | 28 words, under the 35-word cap, but the two filled names are 44 and 46 characters of acronym-dense product title read back to back: "AiVRIC CloudSignals GRCOps and CSPM Onboarding, or M365 Modern GRC System Deployment and Maintenance". Even with the say map turning AiVRIC into av-RICK and M365 into Microsoft three sixty-five, that is a mouthful no listener holds, and the two names are hard to tell apart by ear. | Split into two lines — one per service — so each name lands on its own breath. |
| 19 | minor | ear-test | `kiosk-2` | Circular against the line before it. kiosk-1 has just said "AiVRIC is the platform underneath the managed programs", so "It's the name of the platform" restates the antecedent instead of advancing. The useful half of the line is the second clause, and the module sentence that follows is what the visitor actually needs. | "It isn't a product you buy here. The module running today is CloudSignals+RiskOps." |
| 20 | minor | ear-test | `again-two` | "One still standing" reads as a room left upright rather than a door left unopened — the wrong image for a lobby where the visitor is choosing doors, and slightly ominous. It also breaks the proving/owning/running vocabulary the rest of the node keeps. | "Two rooms walked, one still closed." or name the work: "Two kinds of work seen, one to go." |

## Win Trust  
`door-win-trust` · verdict **defects** · 23 findings

| # | severity | category | line | problem | fix |
|---|---|---|---|---|---|
| 1 | blocker | shelf | `wt-r-run` | The manifest contradicts the line. experience.json programs.isp.category is "Information Security Program & Governance" — a Win Trust family under Candidate F — not "Managed GRC Programs". Only the offer managed-isp carries category "Managed GRC Programs". So §A7's premise ("Managed GRC Programs sits in Stay Ready under F, so wt-r-run says so") is true of the offer and false of the program the line actually speaks. §C never proposes touching programs.isp.category, doors.win-trust.programs (today ["isp","rmp"]) or doors.stay-ready.programs (today ["rmp","cirp","soc"], no isp). The line asserts a room assignment no file supports. | Decide the program's home before the owner signs the line. Either add programs.isp.category → "Managed GRC Programs", move isp from doors.win-trust.programs to doors.stay-ready.programs, and list those two edits in §C; or drop the "lives behind Stay Ready" clause and revert to a wording that does not assert a door. |
| 2 | blocker | manifest-contradiction | `§C3` | §C3 keeps managed-isp as the `then` of a Win Trust start while §A7 and wt-r-run say managed-isp lives behind Stay Ready. check-manifest.js:442 requires every `then` offer to be in `runs` = the door's packages plus its programs' run lists; managed-isp reaches that set only via programs.isp.run, i.e. only while isp stays in doors.win-trust.programs. The two halves of the same document require opposite manifest states, so the chapter cannot pass lint and be true at the same time. | Pick one. If isp moves to Stay Ready, doors.win-trust.starts.*.then must lose managed-isp (all three starts carry it today) and §C must say what replaces it. If isp stays in Win Trust, wt-r-run and the wt-niche sign must stop pointing at Stay Ready. |
| 3 | blocker | shelf | `wt-r-run` | Under Candidate F, Managed GRC Programs is Stay Ready's shelf, so managed-isp is an offer of another room named twice inside Win Trust — once spoken as {program:isp} and once written as {offer:managed-isp}. The draft's own F rule, stated in §A6, signposts a foreign shelf by DOOR TITLE ONLY ("sits behind {door:gain-control.title}, with privacy" — no Gain Control offer is named). The room applies that rule to Gain Control and breaks it for Stay Ready. It also renders the same Builder name through two different token kinds in one beat. | Apply §A6's rule consistently: name the door, not the offer. e.g. line "Then it has to stay true. The program that does that lives behind {door:stay-ready.title}." and sign "Then: {door:stay-ready.title}". If the offer must be named, say why this shelf breach is allowed and Gain Control's is not. |
| 4 | blocker | dead-label | `§A2 / wt Changed-from-today / §C6` | "SaaS & AI vendors" appears twice in §A2 and the wt change note, and all three dead labels ("SaaS & AI vendors", "Portfolio owners", "Regulated operators") appear again in §C6. The rule is that these strings must not appear anywhere in the chapter, in any casing. The document going to the owner for sign-off contains all three verbatim. | Refer to them without quoting: "the retired buyer label on doors.win-trust.icp", "the three hard-coded buyer labels at check-manifest.js:185". The owner can read the strings in the file. |
| 5 | blocker | claims | `§A2` | False against content/tour.json. The `arrive` node's only choice is id "lead", prompt "Would you like {guide:huey} to lead?", remembering "lead" — it sorts the guide, not the visitor's situation. handoff-huey and handoff-avi carry no choice at all. The visitor is sorted ONCE, in `lobby` (choice id "segment"). The same false premise is repeated in the wt change note: "arrival now owns 'what's happening to you'" — arrival owns nothing of the kind. This is the load-bearing argument for cutting wt-1's audience line and for rewording the choice prompt. | Rewrite the argument on what is actually true: the visitor is sorted once, in the lobby, and under F the door is defined by its shelf rather than its audience. Drop the "twice" claim and the "arrival owns it" claim, or move that sort into `arrive` first and say so. |
| 6 | major | shelf | `wt-ai-r2 + wt-r-run` | The AI beat sells written-policies and then, two lines later, sells {program:isp} — whose programs.isp.build is exactly ["is-program-conops-development", "written-policies"]. That is the policy-library double-buy §A10 declares must never happen. check-manifest.js:451 flattens only offers[v].contains, not a program's build list, so the lint will NOT catch it — the script has to. §C4's proposal to add written-policies to the evidence start's `with` (whose `then` is also managed-isp) reproduces the same overlap. | Either stop naming written-policies where {program:isp} follows in the same beat, or state on the record that the program's build phase absorbs it and the two are never invoiced together — and extend the check at 447-451 to flatten programs[x].build so the rule is enforced rather than remembered. |
| 7 | major | claims | `§A3` | No such text exists. grep over content/ finds "finish with where" zero times; the only near match is "That's your door, and where you'd start." at tour.json:3654, in a different chapter. wt-3's real current text is "Tell me what's pressing, and the room will show you." — which the draft itself states correctly in its own change note four sections later. The rationale quotes a line that is not in the file and contradicts the draft's own record of it. | Delete the fabricated quote. Argue the rewrite on its actual merit: today's wt-3 promises a showing without naming what is shown, and "down to the first step you'd take" lines up with the wt-next button "Show me where I'd start". |
| 8 | major | names | `preamble` | False for the document. Running builderTypedNames() against the prose finds plain-text Builder names in §A6 ("AI Governance & Privacy Advisory"), §A10 and §C5 ("SOC 2 Readiness", "ISO 27001 Certification Readiness", "ISO 27701 Privacy Readiness", "PCI-DSS Readiness", "Written IS Policies & Standards") and in the wt-receive change note ("Written IS Policies & Standards"). Door titles are typed too: "Stay Ready" (§A7, §C7), "Gain Control" (§A6). The spoken lines and writes are clean; the blanket claim is not, and it is the claim the owner will trust when skimming. | Either narrow the preamble to "every Builder name and door title in a spoken line or a write is a token", or tokenise the prose too. Do not leave an absolute claim the document breaks eleven times. |
| 9 | major | vocabulary | `wt Changed-from-today` | "Retired" is wrong against the manifest. vocabulary.retired is [Scale Oversight, Stand Ready, Provable Vendor, Provable Vendors, Portfolio Companies, Hue 01, Hue 02, Hue 03] — the buyer label is not on it. It is the opposite of retired: check-manifest.js:185-186 currently HARD-REQUIRES doors[].icp to be exactly ["SaaS & AI vendors","Portfolio owners","Regulated operators"] and errors if it is not. §A2 also calls it "dead" and the change note calls it "retired" — two labels for a status the files do not grant it. | Say what is true: the label is currently mandatory (check-manifest.js:185-186) and this chapter proposes retiring it. Use one word for that state throughout, and add it to vocabulary.retired in §C as part of the removal so the lint enforces the new rule. |
| 10 | major | guides | `all node tables` | Every one of the 33 line rows carries who = "lead". check-manifest.js:762 requires line.who to name a guide id — avi or huey — and errors on anything else; "lead" is not a guide id. §A8 says there are no pinned lines in this chapter, which means the correct representation is to OMIT `who` entirely, exactly as every existing win-trust line in tour.json does. As written the table instructs an invalid key on every line. | Change the column heading to note that these lines are unpinned and the `who` key is absent, or drop the column. Reserve who = avi / huey for a genuinely pinned line. |
| 11 | major | shelf | `wt-receive deal beat` | §A10 and the wt-deal-alt change note both insist the four packages are alternatives, "never as additions", because SOC 2, ISO 27001 and PCI-DSS all carry written-policies. But the room's final state in the deal branch shows all four package names lit simultaneously on panels 3, 4, 5 and 6. A shelf of four lit boxes reads as a set, not as a fork. The picture argues the opposite of the rule. | Make the alternation visible: dim or clear panels 5 and 6 unless the visitor's answer selects them, or write panel 5/6 as "Instead, if…" rather than as bare names, so the room never shows four buyable packages at once. |
| 12 | major | completeness | `§C` | §C is incomplete on its own terms. It never mentions doors.win-trust.programs (["isp","rmp"]) or doors.stay-ready.programs (["rmp","cirp","soc"]), both of which must change if §A7 is true. It never mentions programs.isp.category. It never notes that doors.win-trust.starts.deal and .evidence also carry then:["managed-isp"], not just .ai. The owner is being asked to sign lines whose manifest dependencies are only partly listed. | Add the missing entries: programs.isp.category, doors.win-trust.programs, doors.stay-ready.programs, and the `then` on all three non-early starts. Re-run the §C list against a dry lint before it goes out. |
| 13 | minor | internal-contradiction | `wt-receive Changed-from-today` | Three bullets earlier the same section says "`wt-deal-r1` writes **changed**: `wt-panel-4` gains a second sign line, 'Scope and Statement of Applicability'." wt-deal-r1 cannot be both. An owner reading the summary bullet will approve a line whose writes have in fact moved. | Split the claim: "wt-deal-r1: text unchanged, writes changed (see above)", and list only wt-ev-r1, wt-ev-r2, wt-r-op and wt-early-r1 as untouched. |
| 14 | minor | ear-test | `wt-standards` | 28 words, over the ~25 the ear tolerates, and the second sentence is a verbless fragment ("The privacy one as well:") followed by a colon that stacks two more clauses. Spoken, "the privacy one as well" hangs with nothing to attach to, and "proof is proof" reads as filler on the ear. | Split and give the fragment a verb: "If your buyer named a standard, the package for it is here. The privacy standard too — a certificate is proof, and proof is what this room holds." |
| 15 | minor | ear-test | `wt-ai-r3` | "You can take both" has no clear antecedent. Both rooms? Both programs? Privacy and AI governance? The only two nameable things in the preceding clause are a door and a category, and the visitor cannot hold which pair is meant. The sentence is doing the most important F work in the chapter and is the one a listener will misparse. | Name the pair: "You can take that alongside what you start here." or "You can buy from both rooms." |
| 16 | minor | names | `wt-deal-r1 write` | The second line is a plain-text near-paraphrase of both the Builder category "ISMS, SSPP & Statement of Applicability" and the service "ISMS Scope & SoA Development" (offer id isms-scope-soa-development). It slips past the nameRe check only because the wording is bent, which is precisely the shape the token rule exists to prevent. The draft admits the intent: "without naming a service the package already contains." | Either use {offer:isms-scope-soa-development} and accept that iso-27001-readiness contains it, or pick wording that is plainly not a Builder string, e.g. "What's in scope, and why". |
| 17 | minor | writes | `wt-ai-r2` | The panel attributes "AI risks in the register" to the policy set, but the rewritten wt-ai-r2 no longer mentions a register — the register moved to wt-ai-r1's assessment ("with your AI uses inside the scope"). The panel now shows a deliverable of the previous line under the current one, which is the same misattribution the rewrite was meant to fix. | Move "AI risks in the register" onto wt-ai-r1's write, or replace it here with something written-policies actually produces (e.g. "Standards, by system"). |
| 18 | minor | internal-contradiction | `§C5` | The item asks for a change and then explains why no change is needed. Line 451 already flattens `contains`, so soc-2-readiness + pci-dss-readiness and iso-27001-readiness + pci-dss-readiness already error on written-policies today — as the same paragraph says. No third clause is required for packages. The one real gap (a program's `build` list is not flattened) is the one §C5 does not name. | Retitle it as a no-op confirmation, and replace the body with the actual gap: line 451 flattens offers[v].contains but not programs[x].build, so {program:isp} beside {offer:written-policies} passes lint while double-buying the policy library. |
| 19 | minor | arithmetic | `wt Changed-from-today` | Six, not five. The wt node's room-shows list wt-binders, wt-panel-1, wt-panel-2, wt-panel-3, wt-niche and wt-panel-4. | "the opening beat now uses six." |
| 20 | minor | ear-test | `wt-deal-alt` | Placed between wt-deal-r1 (the lead) and wt-deal-r2 (what the lead contains), so the visitor hears two alternatives and then a contents list, with no signal that the contents belong to the FIRST package named three sentences ago. Spoken in order, "PCI-DSS Readiness. SOC 2 Readiness covers the risk assessment…" invites the listener to attach the contents to the wrong package. | Move wt-deal-alt after wt-deal-r2 so the lead and its contents stay adjacent, or open wt-deal-r2 with a re-anchor: "Whichever you take, the SOC 2 one covers…". |
| 21 | minor | claims | `§A1` | "a hospital group" is a customer characterisation with no source. vocabulary.allowedCustomers is ["Transit Technologies", "a large North American bank"] — the bank half is supportable, the hospital group is invented. It is in rationale rather than a spoken line, but it is the evidence offered for the chapter's biggest cut. | Use only what the manifest allows: "a room that also has to hold a large North American bank buying PCI readiness." Drop the hospital group or get it onto allowedCustomers. |
| 22 | minor | completeness | `wt-proof Changed-from-today` | Not quite true. wt-p2's own `source` field in content/tour.json reads "Transit Technologies, published customer story" — the customer name is written in the script file as well as in the manifest basis, and sources are visitor-reachable through "See the details" ({str:tourDetails}). | "The customer name is on vocabulary.allowedCustomers; it reaches the visitor through the manifest ref, and appears again in wt-p2's source line." |
| 23 | minor | completeness | `wt / all-wt` | Two small omissions the format demanded. The wt choice's `id` ("wt-trigger" in tour.json) is never given, only its `remember` key. And all-wt's change notes never state that all-wt-3's write moves off wt-panel-3 (today a two-item list) onto wt-panel-4, even though the room-shows table changes it — the note only says the line was rewritten. | Add the choice id, and add a bullet: "all-wt-3 write **moved**: the two-item list on wt-panel-3 becomes the four-package list on wt-panel-4; wt-panel-3 now carries the 'What your scope covers' sign." |

## Gain Control  
`door-gain-control` · verdict **defects** · 19 findings

| # | severity | category | line | problem | fix |
|---|---|---|---|---|---|
| 1 | blocker | graph | `gc-trigger` | `trigger-gain-control` is read in eleven conditions OUTSIDE this chapter: nodes.path.lines[6..9] (start-gc-acquisition / -reporting / -control / -early), nodes.start-baseline.lines[0].when[1] (["acquisition","early"]) and .lines[1].when[1] (["reporting","control"]), and summary.lines[8..12] (sum-gc-trigger, sum-gc-acquisition, sum-gc-reporting, sum-gc-control, sum-gc-early). Renaming the option ids silently dead-ends all eleven; "every `when` in this chapter follows" is not enough, and the "What this chapter forces elsewhere" list never mentions path, start-baseline or summary. | Add path, start-baseline and summary to the forced-elsewhere list, with the exact condition rewrites: path lines 6-9 to data/vendors/build/early, start-baseline.lines[0] to ["data","early"] and .lines[1] to ["vendors","build"] (or whatever the new baseline logic is), and summary lines 8-12 renamed sum-gc-data/-vendors/-build. |
| 2 | blocker | shelf | `path / summary` | The chapter cuts Win-Trust-shelf offers from the room but leaves them selling this door everywhere else. nodes.path start-gc-acquisition/-reporting/-control/-early and summary sum-gc-acquisition/-reporting/-control/-early still name {offer:initial-risk-assessment}, {offer:managed-rmp}, {offer:bod-reporting} and {offer:risk-committee-update} as Gain Control's start. start-baseline.lines[1] and [2] also say "the Initial Risk Assessment comes first". After this chapter ships, the tower and the takeaway summary contradict the room the visitor just walked. | Rewrite the four path start-gc-* lines and the five summary sum-gc-* lines onto the F shelf (privacy-leadership-launch / data-mapping-data-inventory, managed-vcp, vulnerability-management-program-development), and re-derive start-baseline: the new `data` start leads with a package whose contains are privacy-maturity, cpo-service, managed-privacy — no risk assessment — so baseline-1 ("that start already includes your baseline") is now false for it. |
| 3 | blocker | claims | `gc-3` | That string does not exist anywhere in content/tour.json, docs/ or the README. Neither does "one clear next step", quoted two sentences later as arrival's promise. The nearest real strings are "That's your door, and where you'd start." (tour.json:3654) and the boilerplate at tour.json:3740. The whole rationale for replacing gc-3 rests on invented source copy. Compounding it, gc-3 is called "the twin of `wt-3`", but wt-3 is "Tell me what's pressing, and the room will show you." — which is gc-4's twin, not gc-3's. gc-3 today is "That gap isn't a philosophy difference. It's a staffing one." | Re-derive the gc-3 decision from the real lines. The twin of wt-3 / sr-4 is gc-4; the collision the draft describes is not in the file, so either quote the real defect or drop the argument and justify the merge of gc-3+gc-4 on the "two bridge lines back to back" ground alone. |
| 4 | blocker | shelf | `gc-data-r4` | As delivered, {offer:ai-governance-advisory} is named in two rooms: wt-ai-r1 ("Here the start is {offer:ai-governance-advisory}...") is untouched in content/tour.json, and gc-data-r4 adds it here. The chapter identifies the collision and hands it to "the Win Trust author" instead of resolving it, so signing this off puts one offer on two shelves — the exact condition Candidate F exists to prevent. | Either hold gc-data-r4 until the Win Trust chapter has cut wt-ai-r1, or state the removal as a hard precondition in the forced-elsewhere table (wt-ai-r1 and its Ask question `ai` both need a replacement start) rather than as an FYI. |
| 5 | major | dead-labels | `—` | All three retired audience labels appear in the chapter: "Portfolio owners" three times (decision 1, the manifest table, and the lint-change bullet) and "SaaS & AI vendors" and "Regulated operators" once each. The rule is that these strings must not appear anywhere, in any casing. A chapter whose thesis is that the labels are gone is the worst place to reprint them — and it is the version the owner reads and the copy sheets are generated from. | Refer to them positionally, not by name: "the three O1 buyer labels asserted at check-manifest.js:185" and "`doors.<id>.icp`, all three doors". No label text needs to appear for either the decision or the lint change to be understood. |
| 6 | major | surfaces | `—` | Wrong, and the whole surface recommendation turns on it. In content/surfaces.json wt-binders has `text: null`, so nothing may be written on it — check-manifest.js errors "carries no text ... give it a glow". Every wt-binders write in tour.json (lines 801, 842, 924) is a bare glow. Win Trust's services list is written on wt-panel-4, which is role `glass`, station **gap** — precisely the "service-shaped thing on a station that is not services" the draft condemns in Gain Control. Gain Control's gc-amber-left (glass, gap) already IS the wt-panel-4 pattern. The proposed fix inherits the error: a "region-role surface with station services, the exact twin of wt-binders" would carry no text and could not "hold a four-item list". The same bullet also calls the new decision surface "the twin of wt-niche" while specifying role `scrim`; wt-niche is role `region`. | Drop the false contrast. Restate the ask as: gain-control needs a text-carrying surface at the services station (a region with a text box, like wt-niche, not like wt-binders), and correct the decision-surface role to `region` if wt-niche is the model, or say plainly it is the sr-panel-3 / sr-rack model instead. |
| 7 | major | completeness | `—` | The manifest table gives the three family names but no `examples`, and gives the four `starts` no `ring`, `source` or `status`. All are lint-required (check-manifest.js ~398-470): each family needs at least one Builder example that has an offers entry, every non-early start needs a non-empty `ring`, every start needs source+status, and — critically — every start's `lead`/`with` must already be listed in a family's examples, packages or programs. `starts.data.with = data-mapping-data-inventory`, `starts.vendors.with = asset-governance-program-development` and `starts.early.lead = data-mapping-data-inventory` will all fail "is in none of ... service families, packages or programs" until the examples are written. As delivered the manifest change cannot be applied. | Supply the examples for each of the three families (they gate the starts), the ring for data / vendors / build, and source+status per start. Also state the final maturityEmphasis list — adding `strengthen` to ["assess","operate","advance"] emphasises all four stages, which is no emphasis at all, so say which one drops. |
| 8 | major | shelf | `gc-data-r5` | It does not. iso-27701-readiness `contains` iso-27701-pims-readiness-implementation, privacy-program-conops-development, data-mapping-data-inventory and data-protection-impact-assessment-dpia — all four are Privacy Management & Data Protection, i.e. this room's shelf. So (a) two services this room sells standalone in gc-data-r3 are carried by a Win Trust package, which is the lint's own "would be bought twice" condition read across doors, and (b) ISO 27701 PIMS Readiness & Implementation sits on Gain Control's shelf by category while gc-data-r5 tells the visitor a privacy certificate is next door. One sentence does not teach a scheme that contradicts itself. | Decide and write down which side owns the four 27701 contents, and make gc-data-r5 say the actual boundary (certificate track vs. running the program), not "privacy certificate = next door". If the split stands, gc-data-r3 needs a guard so a visitor heading for 27701 is not sold the map and the DPIA twice. |
| 9 | major | claims | `all-gc-2` | "Most people here start with" is a frequency claim about 3HUE's own customers, sourced to "3HUE service catalog, September 2026" at status `proposed`. A service catalogue cannot substantiate what most buyers do; there is no engagement data in sources or allowedCustomers to support it. It will pass the figure lint (no numeral) and reach a visitor as a fabricated social proof. | Rewrite without the frequency: "Here the shelf starts with {offer:privacy-leadership-launch} or the {program:vcp}." — same job, no unbacked claim. |
| 10 | major | claims | `gc-decision` | `doors.gain-control.decision` is a bare string. resolveRef (js/tourtext.js) walks the ref chain looking for a `status`; the door object, the doors array and the manifest root all lack one, so the line resolves with status null and source null. The chapter's source table asserts `approved-copy`, which is not what the file says and is not what the tour renders. The same paragraph calls it "the one approved-copy field in this door that Candidate F leaves completely intact", which the manifest does not support. | Either report gc-decision's status honestly as unset, or add the forced-elsewhere item: give doors.gain-control.decision a source and status (all three doors' `decision` fields have the same gap). |
| 11 | major | completeness | `—` | Four nodes change their Ask list with no "Changed from today" entry. Today gc-moment and gc-change are [private-equity, vciso, vendor, rmp], gc-receive is [private-equity, vciso, vendor, packages], gc-proof is [proof, private-equity, vendor, different], all-gc is [private-equity, vciso, vendor, rmp]. The draft changes all five and documents the change only at `gc` and `gc-next`. Since the whole point of the Ask edits is to strip private-equity/vciso/rmp under F, the omission hides the largest single sweep in the chapter. | Add an Ask bullet to the Changed-from-today block of gc-moment, gc-change, gc-receive, gc-proof and all-gc, each naming what left and why. |
| 12 | minor | ear-test | `gc-data-r3` | Resolved it is 26 words — "Before any of that, someone has to draw the map: Data Mapping & Data Inventory, and Data Protection Impact Assessment (DPIA) where the use is sensitive." Two long formal service names in one breath, with a trailing condition ("where the use is sensitive") that a listener cannot attach without rewinding. It passes the 35-word lint and fails the ear. | Split it: "Before any of that, someone has to draw the map: {offer:data-mapping-data-inventory}. Where the use is sensitive, {offer:data-protection-impact-assessment-dpia} goes with it." |
| 13 | minor | ear-test | `gc-bld-r3` | Resolved, the subject and verb disagree out loud: "Security Architecture Reviews puts eyes on the design". A plural Builder name taking a singular verb is audible and reads as an error when spoken by a synthetic voice. gc-ven-r4 has the same shape in reverse: "the program widens a block of vendors at a time" has no antecedent — the last program named was {program:vcp}, three lines earlier, and "the program" in this line most naturally attaches to Audit Support. | Recast around the name: "A {offer:security-architecture-reviews} puts eyes on the design before it ships..." or "{offer:security-architecture-reviews} put eyes on..." if the plural is intended. For gc-ven-r4, name it: "And the {program:vcp} widens a block of vendors at a time." |
| 14 | minor | ear-test | `gc-2` | "The estate gets bigger than the head" asks the listener to carry a metaphor whose second term ("the head") was a body part one sentence ago and is now a stand-in for a person's memory. Said aloud it lands as a non-sequitur. gc-ch-2's proposed replacement has a related problem — "applied the same way everywhere it lands, and kept separate where it has to be" stacks a sameness clause against a separateness clause in one 19-word sentence, which is the same collision decision 3 claims to be fixing in gc-3. | gc-2: "For a while one person holds all of it in their head. Then there is more of it than one person can hold." gc-ch-2: consider two beats — one control set, then the isolation guarantee. |
| 15 | minor | claims | `gc-p2` | Two issues. "That's the only way" is a causal exclusivity claim the source (a published customer story) does not make, yet it is filed as `adapted` rather than `derived` or `proposed`. And "a number like that" is a deictic reference to the figure on the card; if the card is dismissed, or the line is heard on audio replay, the reference is orphaned. | Downgrade to `derived`, and anchor the reference: "One framework, applied the same way every time. That is what makes a count like that possible." (or restate the mechanism without pointing at the figure). |
| 16 | minor | completeness | `—` | True but partial. This chapter also speaks {offer:data-protection-impact-assessment-dpia} ("DPIA"), {offer:secure-sdlc-program-development} ("SDLC"), and Privacy Leadership Launch's contents include CPO-as-a-Service. guide.voice.say holds only AiVRIC, 3HUE, SOC 2, vCISO, POA&M, BOD, TTX, M365 and NIST CSF — no DPIA, SDLC or CPO. Nothing in the lint catches a missing say entry, so whatever is not listed here will ship mispronounced. | Extend the voice note to DPIA and SDLC alongside VCP, and check CPO if the package contents are ever spoken. |
| 17 | minor | accuracy | `—` | gc-receive names nine offers, not ten (privacy-leadership-launch, data-mapping-data-inventory, data-protection-impact-assessment-dpia, ai-governance-advisory, asset-governance-program-development, audit-support, vulnerability-management-program-development, security-architecture-reviews, secure-sdlc-program-development) plus two programs. Small, but it is the count that carries the surface argument. Two related slips: gc-p2's old write on gc-amber-right was a `sign`, not a card (the draft says "carded onto gc-amber-right"); and the header's "5 measured surfaces" counts gc-console, which surfaces.json marks `target: false` with no station and `text: null`. | Correct to nine offers, say `sign` for the old gc-p2 write, and qualify the header: five measured, four targetable, three carrying text. |
| 18 | minor | surfaces | `—` | The extent is about right against the render, but the plane is not clear. In media/rooms/gain-control-2560.jpg that quad is crossed by the centrepiece planter and document stacks along its spine and, more seriously, by the foreground chair back, which occupies roughly x 860-1160, y 840-1320 and hides the near third of the table. The draft gives occluders only for the right-hand wall. A raked, one-fifth-of-frame-tall plane at y 740-940 also gives very little legible text height. | Either declare the table-top occluders in the ask (planter, stacks, foreground chair back) and check the remaining text box is usable, or propose the credenza face / the left amber niche instead. The dark right-hand wall proposal is sound; note only that its left edge at x~1800 abuts the wood pillar. |
| 19 | minor | schema | `gc-ch-2` | Status is not per-field. `doors.gain-control.program` is a single object carrying one `source` and one `status` for `start`, `build` and `operate` together; resolveRef finds the status by walking up to that object. The chapter rewords all three sub-fields, so there is one downgrade, not two independent ones — and it also silently moves `program.start` off approved-copy even though the chapter says that field is "not quoted by the script". | State it once: rewording any of doors.gain-control.program.{start,build,operate} moves the whole block to `proposed`, taking gc-ch-2 and gc-r-op with it. |

## Stay Ready  
`door-stay-ready` · verdict **defects** · 22 findings

| # | severity | category | line | problem | fix |
|---|---|---|---|---|---|
| 1 | blocker | names | `sr-inc-r1, sr-inc-r2, sr-inc-r4, sr-inc-r5, sr-det-r1, sr-det-r4, sr-run-r2, sr-run-r3, sr-early-c1, all-sr-2` | Roughly eighteen Builder names are typed as plain text in the "Room shows" writes instead of tokens: Cyber-Incident Response Plan Development, IR Playbook Development, Cyber-IR Tabletop Exercise (TTX), Incident Response Fast Start, Business Continuity Plan Development, Managed Cyber-Incident Response Program, MXDR Complete Protection, MXDR Starter, MXDR — SIEM Add-on, Managed Security Controls Validation for Endpoints, Managed Risk Management Program, Managed Information Security & Privacy Management. Every one of these positions uses a token in today's content/tour.json. tools/check-manifest.js runs checkText on each write item (line 738) against builderTypedNames (line 573), so each is a hard error: "typed Builder name ...; use a token so the manifest stays the only place it is written". The draft's parenthetical about showing tokens inline covers the spoken lines, not the writes — and sr-inc-r1's own table row types the name in the line text too. | Write every one of these as {offer:<id>} / {program:<id>}: {offer:ir-plan}, {offer:ir-playbook}, {offer:ir-ttx}, {offer:ir-fast-start}, {offer:bcp-development}, "Then: {offer:managed-cirp}", {offer:mxdr-complete}, {offer:mxdr-starter}, {offer:mxdr-siem-add-on}, {offer:managed-security-controls-validation-for-endpoints}, {offer:managed-rmp}, {offer:managed-isp}. Set sr-inc-r1's line text to "The start is {offer:ir-fast-start}: ..." and move the spelled-out reading into the parenthetical note only. |
| 2 | blocker | shelf | `manifest follow-up 6` | rmp and isp are already doors.win-trust.programs = ["isp","rmp"], so this puts two programs in two rooms. Worse, the lint's `listed` set (check-manifest.js line 417) is families + packages + every program's build and run. programs.rmp.run = [managed-rmp, risk-register-poam, annual-risk-update, rmp-review] and programs.rmp.build = [initial-risk-assessment, risk-management-program-conops-development] — all Risk Assessment & Risk Management, i.e. Win Trust's shelf under F, including risk-register-poam, the exact offer this chapter cuts with "move to Win Trust". programs.isp.build/run = [is-program-conops-development, written-policies, managed-isp, is-program-review] — Information Security Program & Governance, also Win Trust's. The follow-up therefore re-imports onto Stay Ready's panel everything the chapter just evicted. | Do not add rmp or isp to doors.stay-ready.programs. Use programs = ["cirp","soc"] and get managed-rmp and managed-isp onto the shelf through the Managed GRC Programs serviceFamily examples instead (their offer category is Managed GRC Programs, so they pass the family-category check). If starts.run needs a `then`, point it at a package or at cirp/soc's run list, not at rmp. |
| 3 | blocker | shelf | `manifest follow-ups` | The follow-up list is not the full cost. doors.gain-control.serviceFamilies still contains "Managed GRC Programs" with examples "Managed Risk Management Program" and "Managed Information Security & Privacy Management", and doors.gain-control.starts.control has lead "managed-rmp" with "managed-vcp"/"vciso". Under Candidate F Managed GRC Programs is Stay Ready's. As drafted, managed-rmp and managed-isp are sold in two rooms — the defect the chapter's own sr-exam-r1/r2/r3 cuts were made to prevent. (doors.gain-control.serviceFamilies also still lists Risk Assessment & Risk Management, which is Win Trust's under F.) | Add a follow-up: doors.gain-control.serviceFamilies must drop Managed GRC Programs (and Risk Assessment & Risk Management), and doors.gain-control.starts.control must be re-led off managed-rmp, otherwise sr-run-r2 and sr-run-r3 sell offers Gain Control also sells. |
| 4 | major | guides | `sr-inc-r3` | Factually wrong. content/tour.json pins Huey on four lines: arrive-4 ("Hi. Glad you're here."), handoff-huey-2 ("Thanks, {guide:avi}. Let's find your door."), handoff-avi-1 ("I'll be at the round table when you're done.") and close-back-huey ("Welcome back."). The stated justification for the chapter's only pin rests on a false premise the owner will be asked to sign off on. | Correct the claim to "Huey is pinned on four lines today, all of them at arrival and the close" and re-argue the pin on that basis, or drop it. |
| 5 | major | guides | `sr-inc-r3` | The pin breaks in both configurations. If the visitor answers the `lead` choice at `arrive` with Huey, `who: "huey"` on sr-inc-r3 is simply the lead speaking — there is no second voice and the whole stated effect disappears, with no fallback written. If the visitor stays with Avi, handoff-avi-1 has Huey say "I'll be at the round table when you're done" — so Huey cutting in inside the Stay Ready room contradicts the arrival setup. Either way the line does not hold up. | Either pin the line to the non-lead guide dynamically (needs a second line pair conditioned on `answers.lead`, as close-back-huey/close-back-avi already do), or leave sr-inc-r3 unpinned and let the lead say it. |
| 6 | major | coherence | `sr-2` | This is the chapter's one line stating what the room holds, and it is contradicted by the room's own shelf. Incident Response Fast Start contains ir-plan (Cyber-Incident Response Plan Development) and ir-playbook — writing the plan is what the room's flagship package does. bcp-development writes the continuity plan. And sr-det-r4 says outright "if the plan for the bad night isn't written yet, {offer:ir-fast-start}". A listener who buys sr-2 will be confused four nodes later. | Say what is actually true of the shelf, e.g. "This room is about what happens when the plan gets used. Response. Continuity. Detection. And the programs that keep all three current." — which survives ir-fast-start writing the plan. |
| 7 | major | coherence | `sr choice` | Three of the four replacement labels are themselves situations: "Something happened, or nearly did", "Nobody is reading the alerts", "It's all written down. Nobody runs it.", sub "Nothing on the calendar forcing it". The stated design rule ("The lanes are now what you are buying, not who you are") is not met — the door still sorts the visitor by situation, using different words. sr-4's "Say which one you'd start with" also does not fit: a visitor cannot start with a situation, and the four things sr-2 just named (Response / Continuity / Detection / programs) are not what the buttons offer. | Either label the options by what is on the shelf ("Response and continuity", "Detection, run for you", "The managed programs", "Not yet") so sr-4 and sr-2 line up, or drop the claim that situations have left the door and say plainly that the door still asks one situational question. |
| 8 | major | redundancy | `sr-det-r2` | Near-verbatim repeat of sr-det-c1 two nodes earlier: "After: someone outside your team reads what your tools are saying, and answers it." A detect-lane visitor hears essentially the same sentence twice, once as the change and once as the offer. | Cut sr-det-r2 or replace it with something the after-beat did not already say — e.g. what arrives on the customer's side (a named contact, a written escalation path). |
| 9 | major | ear-test | `sr-det-m1, sr-det-m2` | Two consecutive lines in the same lane use the identical construction "What's missing is someone … them." Read aloud back to back it sounds like a stutter, and it burns the house line (Win Trust's "The frameworks are public. What's missing is someone to run them.", tour.json line 693) twice in one breath rather than once per room as the draft claims. | Keep sr-det-m2's house line and rewrite sr-det-m1's second clause, e.g. "Nobody's job is to read them." |
| 10 | major | grammar | `sr-det-r3` | Compound subject ("What it covers" and "who gets called") with a singular verb — should be "are scoped". It is also the hardest sentence in the chapter to say aloud: two nested clauses between the subject and the verb, with a comma pair the voice has to hold open. This is the line the draft designates as the single place the coverage question is answered, so it has to be clean. | Split it: "What it covers is scoped with you. So is who gets called when something fires." |
| 11 | major | surfaces | `sr-proof, sr-receive, sr-next` | Nothing turns it off. sr-change's sr-ch-3 sets the dial to "No gap" (and the incident lane's sr-inc-r3 sets "Now"), and sr-receive is declared with no node base state while sr-proof's base state lists only sr-monitor-1/2/3. So the dial carries "No gap" through the whole of sr-receive for the detect, run and early lanes, and carries "No gap" or "Now" through sr-proof and sr-next. The room shows its clean after-state while the shelf and the proof are being read — the opposite of the chapter's "Never decoration" rule, and it silently contradicts "the only place the dial reads clean". | Add `sr-clock: null` to sr-proof's and sr-next's node base state, and decide explicitly what the dial reads in sr-receive per lane (null for detect/run/early, "Now" for incident) by putting sr-clock in sr-receive's base state. |
| 12 | major | coherence | `sr-proof` | Contradicts the chapter's own work. Every other node drops exam-findings; follow-up 8 says exam-findings "now spans two doors and should say so". Meanwhile the actual ask answer (ask-exam-1) names {offer:controls-gap-assessment} and {offer:risk-register-poam} — both moved to Win Trust by this chapter — and the question carries `goto: "sr"`, which routes the visitor straight back into this room for work it can no longer sell. | Replace exam-findings with mdr or remediation-owner in sr-proof's suggestions, and add the `goto: "sr"` re-target to follow-up 8. |
| 13 | major | names | `sr-early-c1` | The prose says the surface carries a token; the Room shows table for the same line types it out: `sr-monitor-2` → sign "Cyber-IR Tabletop Exercise (TTX)". The owner cannot tell which one is being proposed, and the table version is a lint error. | Make the table read `{offer:ir-ttx}`. |
| 14 | major | shelf | `sr-p3` | The chapter's whole argument is that a door is a shelf and another door's goods do not belong in it, yet it keeps Gain Control's proof as this room's run-lane proof. The content also pulls the wrong way: "certified under one scalable assessment framework" is a certification-at-scale story — an assessment argument, which under F is Win Trust's — not evidence that anything was kept running. The draft's own gloss ("one framework operated across a very large estate") is an interpretation the manifest basis ("A large North American bank, published customer story, client unnamed") does not support. | Cut sr-p3 and let the practitioner record carry all four lanes, or replace it with a run-lane proof sourced to this door. If it stays, say plainly in the follow-ups that Stay Ready is borrowing Gain Control's proof and the owner must approve that. |
| 15 | minor | completeness | `manifest follow-up 3` | Incomplete against the lint. Every non-early start needs a non-empty `ring` of stage ids (check-manifest.js: "ring: name the stage the tower lights") and a `trigger` integer indexing doors.stay-ready.triggers, with exactly one start per trigger. The follow-up gives leads and withs but no rings and no trigger indices, and follow-up 2 leaves the trigger sentences themselves unresolved — so as written the manifest still will not pass. | State the ring for each new start (they must also be in maturityEmphasis: strengthen / operate / advance) and give the three rewritten trigger sentences plus their indices. |
| 16 | minor | claims | `Claims discipline paragraph` | The sentence asserting the held facts are absent types four of them into the document. The rule is that they must not appear at all, in a document that goes to the owner and may be quoted or pasted onward. The same pattern appears in follow-up 1, which types the dead label: "'Regulated operators' is quoted by the lobby's option labels today." | Say "the held figures and the retired buyer label appear nowhere in this chapter" without reproducing them, and refer to the manifest field path (doors.stay-ready.icp) rather than its value. |
| 17 | minor | ear-test | `sr-early-c1` | 27 words with three joints — a conditional opener, a superlative noun phrase with an embedded clause ("the quickest way to find out where you stand"), then a colon and a two-part imperative. A listener has to hold "when it comes" across the whole thing before the payoff arrives. | Split into two: "When it comes, the cheapest way to find out where you stand is a tabletop exercise. Run the day on paper before you run it for real." |
| 18 | minor | ear-test | `sr-run-r4` | "them" refers back to a singular "an examiner", and can equally be heard as referring to the findings. "sits behind Win Trust" is spatial jargon that only works for a visitor who has already understood the doors as rooms; a first-time listener hears a location, not a shelf. | "If an examiner is driving this, the assessment that answers the finding is bought in {door:win-trust.title}. This room is what keeps it closed." |
| 19 | minor | coherence | `sr-run-r1` | "The start" is then given as three different offers (sr-run-r2 names two, sr-run-r3 a third), while follow-up 3 sets a single starts.run lead (managed-rmp). The other lanes name one start; this lane names one, then three. A visitor asked "where would I start" gets no answer. | Name the lead: "The start here is {offer:managed-rmp}: someone owns the reviews, the register and the dates." and let r2/r3 add the other two as what runs beside it. |
| 20 | minor | completeness | `sr-1, sr-3, sr-4, sr-p2, sr-early-c1, sr-early-m1, sr-early-n, sr-early-r1, sr-inc-r2, sr-inc-r5, all-sr-2` | Eleven line ids are reused for entirely different copy. tools/check-manifest.js notes line ids are unique across the file because voice files are named by them (media/voice/<id>.mp3), and sr-1 currently says "This room is for banks, credit unions, lenders and operators…". No voice manifest exists yet (the lint warns all 219 spoken lines run captions-only), so nothing breaks today, but the moment recording starts, an id-for-id diff will silently ship the wrong audio and the change log loses the trail. | Retire the old ids and mint new ones for new copy (sr-open, sr-holds, sr-stat, sr-ask), keeping an id only where the text is genuinely unchanged. |
| 21 | minor | completeness | `sr-2, sr-inc-r4, sr-early-r1` | These three lines carry writes but the draft never states which surface they frame, while nearly every other line in the chapter does. sr-2 in particular writes four surfaces and is the node's inventory line, so the missing frame is the one the room most needs. (A cue is optional in the schema — today's sr-exam-r4 and sr-inc-r2 have none — but the chapter is inconsistent with itself.) | State a frame for each, or say explicitly that these three deliberately frame nothing. |
| 22 | minor | accuracy | `sr-cross-n, sr-exam-r2` | Misquote. The real line is "Your next step: name the obligation and who it lands on. The team scopes from there." — the second sentence is dropped with no ellipsis, so the owner is signing off on a cut whose full text they have not seen. The sr-exam-r2 quote does use an ellipsis ("every finding gets an owner and a date…"), which conflicts with the completeness rule against ellipses. | Quote both cut lines in full. |

## Tower  
`path` · verdict **defects** · 17 findings

| # | severity | category | line | problem | fix |
|---|---|---|---|---|---|
| 1 | blocker | graph | `start-people-1 / start-people-2 / start-platform-1 / start-platform-2 / baseline-people / baseline-2 / review-platform / review-people` | The stated fix is impossible and the underlying home does not exist. nodes.arrive.choice already carries remember: "lead" (the guide pick), and lintChoice (check-manifest.js:790-825) allows exactly one `remember` per choice, so the arrival choice cannot also remember `situation`. Worse, there is no round-table node and no kiosk node anywhere in tour.json — the node set is arrive, handoff-huey, handoff-avi, lobby, lobby-again, wt/gc/sr and their moment/change/receive/proof/next nodes, all-wt/gc/sr, path, start-baseline, start-review, start-running, path-all, close. No visitor can ever hold situation = people or situation = platform. Eight new lines are therefore dead, and every one of their `when` blocks errors at checkCond (:641) with "no choice remembers situation". | Do not present these eight lines as copy pending a one-line `when` change. Either (a) hold them back until the arrival chapter gains a NEW situation node with its own choice and the graph gains round-table and kiosk entries, or (b) rewrite the dependency note to say plainly what is required: a new choice node, new option ids, new inbound edges to `path`, and a decision on how a people/platform visitor reaches the tower at all. |
| 2 | blocker | shelf | `path-all-gc / path-all-sr / review-gc` | path-shelf — the draft's own answer to the F shelf split — exists only in node `path` and is gated on `door`, so the two places that cross shelves hardest never carry it. In path-all, `path-all-gc` names Initial Risk Assessment and `path-all-sr` names Periodic Controls Gap Assessment; both are Risk Assessment & Risk Management, which is Win Trust's shelf under F, and path-all has no shelf line at all. In start-review, `review-gc` names RMP CONOPS Review & Update and Annual Risk Assessment Update — again both Risk Assessment & Risk Management, i.e. Gain Control's entire review answer sits on Win Trust's shelf — and the draft declares that node already correct. (review-wt and review-sr are genuinely on-shelf; only review-gc is wrong.) | Add a path-all variant of path-shelf after path-all-wt/gc/sr, and either add a shelf line to start-review or re-scope review-gc to Gain Control's F shelf (Privacy Management & Data Protection, Vendor & Third-Party Risk Management, Security Engineering & Architecture). Remove the claim that start-review is already correct. |
| 3 | major | manifest-claim | `reasoning 8` | The rule-441 list is wrong in both directions. `listed` (check-manifest.js:435) is family examples UNION packages UNION the door's own programs' build+run. gain-control.programs includes `rmp`, whose build/run contains initial-risk-assessment, so gain-control.starts.acquisition.lead does NOT fail 441 — that entry is an over-claim. Four real failures are missing: win-trust.starts.ai.lead = ai-governance-advisory (Privacy Management moves to Gain Control), gain-control.starts.reporting.with = risk-committee-update, stay-ready.starts.crossing.with = ai-governance-advisory, and stay-ready.starts.early.lead = controls-gap-assessment. Separately unmentioned: check-manifest.js:175 errors unless a door lists exactly four service families, and under F both Win Trust and Gain Control have three. The owner is being asked to sign off on a migration estimate that is materially understated. | Replace the four-item list with the verified set (win-trust.starts.ai.lead; gain-control.starts.reporting.lead and .with; stay-ready.starts.exam.lead; stay-ready.starts.crossing.lead and .with; stay-ready.starts.early.lead), drop gain-control.starts.acquisition.lead, and add the serviceFamilies-length rule at line 175 to the list of things the F migration breaks. |
| 4 | major | stagecraft | `start-people-2 / baseline-people / review-people / running-2` | The people branch tells the visitor the ring is not theirs, and then all three follow-on nodes light a ring segment for them anyway, from the node's own scene, unconditionally. start-baseline is {kind:"path", stage:"assess"} — the draft's own Room shows says it is "held for every line in the node" — so Assess burns while baseline-people says "There's nothing to baseline yet." start-review lights Strengthen under review-people. And the draft's NEW start-running scene {kind:"path", stage:"operate"} lights Operate for a people visitor who was just told the ring isn't what they're buying. The Room shows blocks for all three nodes state the segment is held for every line and never flag the collision. | Either drop start-people-2's claim about the ring (the draft already offers this trade in the path-1 note), or make the people branch skip the staged nodes. Do not leave a line that denies the ring playing against three lit ring segments. |
| 5 | major | repetition | `start-sr-exam` | This is a near-verbatim repeat of sr-receive's sr-exam-r2, which the same visitor heard one chapter earlier: "Then, under {offer:risk-register-poam}, every finding gets an owner and a date, and stays tracked until it closes." The added "on what's actually running" also echoes sr-exam-r1 ("sampling what's actually running"). The tower is meant to compress the walk, not replay it. The draft presents the borrowing as a virtue ("the concrete promise the room already made in sr-exam-r2") without noticing it produces a duplicate sentence inside one walk. | Keep the ear-test fix but re-voice the second sentence so it summarises rather than repeats — e.g. name the register as what carries the findings forward, without restating the owner-and-date promise the room already delivered. |
| 6 | major | dangling-reference | `running-1` | running-1 is unconditional but refers to "the start" — a start that was never named for two of the draft's own visitors. A situation = people visitor was just told "the first step isn't a stage on this ring", so no start stands. An Ask-jump visitor arriving through where-to-start's goto: "path" with no door, no trigger and no situation heard no start line at all. The draft identified exactly this hole and fixed it in start-baseline with baseline-open, and in reasoning 11 states the Ask jump is why path and start-baseline needed unconditional openers — then left start-running (and, less severely, review-1's "start by reviewing what you have") unpatched. | Give start-running the same treatment as start-baseline: an unconditional opener that does not presuppose a named start, with running-1 gated on `door` (or on a start having been named). |
| 7 | major | shelf | `start-wt-ai / start-sr-crossing` | AI Governance & Privacy Advisory is named in a Win Trust line (start-wt-ai) and again in a Stay Ready line (start-sr-crossing) — an offer in two rooms. Under Candidate F it belongs to neither: its Builder category is Privacy Management & Data Protection, which moves to Gain Control. Both lines are carried forward as "unchanged, word for word" with no note, and reasoning 7 mentions the AI Governance crossing only in passing while listing which offers move. | Call out ai-governance-advisory explicitly as the one offer this chapter names from two different doors, and confirm with the owner that path-shelf alone is enough cover for a name that is off-shelf in both places it appears. |
| 8 | minor | ear-test | `start-sr-exam` | The rewrite is longer than the line it claims to shorten: 30 words filled versus 28 for today's version (measured with the real fillTemplate). It also keeps the fronted subordinate clause it was rewritten to remove — "Then, under Manage Risk Register & POA&M, …" still stacks a prepositional clause containing an ampersand and an acronym in front of the subject. "actually" is filler. | Lead with the subject: "Then the register carries every finding, with an owner and a date." Drop "actually". Correct the rationale, which currently claims a 27-word original and a shortening that did not happen. |
| 9 | minor | ear-test | `start-platform-1` | 26 words carrying two long product names with five acronyms and two ampersands between them, then a conditional tail. A listener cannot hold both names long enough to choose between them, and a voice file has to read "GRCOps & CSPM" aloud. | Split into two sentences, or name one platform per sentence with the condition first: "If the program runs on AiVRIC, it starts with the CloudSignals onboarding. Otherwise it's the M365 deployment." (tokens preserved). |
| 10 | minor | ear-test | `path-shelf` | Spoken, "behind a door" reads as hidden or blocked, which is the opposite of the intended meaning (it is available, just filed elsewhere). The visitor has literally just walked through a door, so the spatial metaphor competes with the room they were standing in. The whole line is 26 words in two clauses. | Say the filing, not the geography: "So your first step may be filed under a door you didn't walk." or "…may come from another door's shelf." |
| 11 | minor | contradiction | `path-shelf` | This is false against the lobby the visitor answered two chapters earlier. nodes.lobby.choice labels its three door options {door:win-trust.icp}, {door:gain-control.icp}, {door:stay-ready.icp} — the three dead labels — which sorts precisely by who you are. Reasoning 1 correctly notes this chapter avoids .icp, but never flags that path-shelf's claim is untrue until the lobby's own labels are replaced. | Add a dependency note: path-shelf cannot ship before nodes.lobby.choice stops labelling its options with {door:*.icp}. Otherwise the tower contradicts the lobby. |
| 12 | minor | ui-mismatch | `path-all-1` | The change from "one path" to "one route" breaks a deliberate echo. strings.legend = "Three doors, one path" is the lobby chapter title AND the sub of close's "Walk another door" option — which the visitor reads on the very next screen after path-all → close. strings.pathChip = "See the maturity path" and path.share.description also say path. path-all-1 was quoting the legend; the revision leaves the spoken line and the on-screen chip disagreeing. | Either keep "one path" in path-all-1, or extend the owner decision in reasoning 4 to cover strings.legend and strings.pathChip so the whole vocabulary moves together. |
| 13 | minor | completeness | `start-platform-2` | start-platform-2 is the only one of node path's 21 lines with no stagecraft stated. The Room shows block accounts for the other 20 — each is assigned a ring segment or explicitly listed as carrying no cue (path-1/2/arc unlit; three early lines and both people lines no cue; path-shelf and path-price no cue) — and silently omits start-platform-2. | Add start-platform-2 to the no-cue list (Operate presumably stays lit under it, as with path-price). |
| 14 | minor | manifest-claim | `reasoning 4` | The owner decision is scoped too narrowly. Reordering to Assess → Strengthen → Advance → Operate also breaks doors[].maturityEmphasis: gain-control is [assess, operate, advance] and stay-ready is [strengthen, operate, advance], both of which are in canonical order today and would no longer be. js/panel.js:64 and :154 render emphasis in m.stages order, and walk.js:26 builds the tower walk from m.stages, so the reorder changes three rendered surfaces the note does not mention. | Extend the owner decision to name doors[].maturityEmphasis, js/panel.js and js/walk.js, or state that the emphasis lists are unordered sets and the render is unaffected — but say which. |
| 15 | minor | source-drift | `start-platform-2` | The cited source (Message Stack §09 boilerplate; §01 core narrative, via ask-aivric-1) makes that claim about AiVRIC specifically: "AiVRIC is the platform 3HUE's managed programs run on, so the evidence stays current between audits." Generalising it to "a platform" asserts it of any third-party tool, which the approved Ask answers contradict in emphasis: ask-platform-1 says "A platform tells you what's wrong" and ask-platform-2 says 3HUE works "on top of the tool you already bought." | Either restrict the claim to AiVRIC, or re-source the line to ask-platform-1/-2 and re-voice it to match what those say a platform does. |
| 16 | minor | terminology | `reasoning 2 / reasoning 6 / reasoning 9` | "Round table" and "kiosk" each mean two different things inside one document. In reasoning 2 the round table is the tour's close chapter (chapters[6].title = "The round table", the guides' reunion, landmark lobby); in reasoning 6 and 9 it is a Candidate F shelf of 33 people services with its own visitor situation. Likewise experience.json `kiosk` is the AiVRIC intelligence-layer display (derived counts of Buyer segments / Maturity stages / Service families), not a Technology Platform shelf. Neither F home exists in the graph, so an owner reading reasoning 2 and reasoning 6 will read them as the same place. | Use distinct words for the F shelves and the existing tour landmarks, and state once, up front, that neither F home is in the graph yet. |
| 17 | minor | rationale | `baseline-1` | It does not. Today's branch 1 is {door: win-trust, trigger-win-trust: [deal, evidence]} and branch 3 is {door: win-trust, trigger-win-trust: [early]}; `early` appears nowhere in branch 1. The merge itself is behaviour-preserving, but the stated reason is false, and an owner checking it will find the claim does not hold. | Restate as "branches 1 and 3 share the same door, so their trigger lists merge" — which is true and is what the draft actually did. |

## Close and summary  
`close` · verdict **defects** · 18 findings

| # | severity | category | line | problem | fix |
|---|---|---|---|---|---|
| 1 | blocker | shelf | `sum-wt-deal, sum-wt-evidence, sum-wt-ai, sum-gc-acquisition, sum-gc-reporting, sum-gc-control, sum-gc-early, sum-sr-exam, sum-sr-crossing, sum-sr-early` | This is the load-bearing claim of the chapter and it is false under Candidate F. Ten of the twelve door-keyed lines recommend offers whose Builder category puts them behind a different door. Checked against experience.json categories: managed-isp is Managed GRC Programs = Stay Ready, yet it closes three Win Trust lines (sum-wt-deal, sum-wt-evidence, sum-wt-ai). ai-governance-advisory is Privacy Management & Data Protection = Gain Control, yet it is the whole of sum-wt-ai and half of sum-sr-crossing. initial-risk-assessment and risk-committee-update are Risk Assessment & Risk Management = Win Trust, and bod-reporting is Information Security Program & Governance = Win Trust, yet all three carry Gain Control lines (sum-gc-acquisition, sum-gc-reporting, sum-gc-early). managed-rmp is Managed GRC Programs = Stay Ready but appears only in Gain Control lines. controls-gap-assessment and risk-register-poam are Win Trust but carry both Stay Ready lines (sum-sr-exam, sum-sr-early). privacy-leadership-launch is the Gain Control package but leads sum-sr-crossing. Only sum-wt-early and sum-sr-incident are clean. The email a visitor forwards therefore states one door's promise and then recommends another door's shelf, which is exactly the thing F exists to stop. | Do not sign these twelve lines off as unchanged. Re-derive each one from the Candidate F shelf for its own door, or state plainly in the sheet that ten of them must be rewritten before F lands and that this chapter's sign-off is conditional on that. If a cross-room recommendation is intentional (a first step that legitimately sits behind another door), the line has to say so in words the visitor can follow, not present it as this door's shelf. |
| 2 | blocker | shelf | `who-owner-2` | {offer:ciso-support} resolves to "CISO Support", whose category in experience.json is "Information Security Program & Governance" — a Win Trust category under Candidate F, not one of the eight people categories that make up the round table. The draft notices this and files it as a parenthesis rather than a defect ("`ciso-support`, which I also use, sits under Information Security Program & Governance"). It is a defect: the round table names a Win Trust offer, and if the Win Trust shelf also names it the offer lands in two rooms. | Either say the fractional-executive shape in plain words only (the draft already proves that is possible and better for the ear), or add a Fractional Executive & Retainer Services offer id and name that instead. Do not carry a Win Trust offer as the round table's headline example. |
| 3 | major | shelf | `sum-wt-evidence / sum-gc-acquisition / sum-wt-ai / sum-sr-crossing` | Two offers appear in two different rooms inside the same summary. initial-risk-assessment carries sum-wt-evidence and sum-wt-early (Win Trust) and also sum-gc-acquisition and sum-gc-early (Gain Control). ai-governance-advisory carries sum-wt-ai (Win Trust) and sum-sr-crossing (Stay Ready) while its own category, Privacy Management & Data Protection, makes it a Gain Control offer — three rooms implicated for one service. A visitor who walks two doors receives an email that recommends the same starting service under two different door headings, which reads as a template rather than a recommendation. | Pick one room per offer and say so in the sheet. Where a service genuinely is the first step for two doors, the second line should refer back ("the same first step as above") rather than re-name it, so the email does not read as boilerplate. |
| 4 | major | correctness | `close-3` | It no longer does. The same bullet adds a hideWhen on option 3: "**Hidden once all three door chapters have been visited**". js/tourtext.js matches() ANDs the `visited` list, so the button disappears for exactly the visitors who walked all three doors — and for every "Show me everything" visitor, since all-wt, all-gc and all-sr carry chapters door-win-trust, door-gain-control and door-stay-ready. Those visitors hear the lead say "walk another door" while no such button is on screen. The two changes in this bullet contradict each other. | Either condition close-3 the way close-people is conditioned (a three-option variant for the all-doors visitor) or drop the hideWhen. Do not ship a spoken line that names a control the same visitor cannot see. |
| 5 | major | claims | `who-owner-1` | The line offers three fractional executives; only two exist on the live catalog. builder-names.json, Fractional Executive & Retainer Services, holds Virtual CISO — Support, Virtual CISO — Fractional, Fractional DPO — SOW & Retainer, Senior IS Consulting Retainer and Incident Command & Emergency Response Leadership. There is no technology-leadership fractional anywhere in the non-draft capture: the vCIO offering ("Virtual CISO (vCIO)" as a category, "vCIO-as-a-Service" as a service) sits in builder-names.json `draft`, which the file's own note says is "never on the site". Saying it in lowercase words rather than as a Builder name evades the lint but still sells a draft-catalog service on a public surface. The follow-on line compounds it: who-owner-2 and who-owner-3 answer only the security branch, so privacy and technology leadership are promised and then dropped. | Cut "or technology leadership" until a non-draft fractional technology executive exists, or get the owner to confirm on the record that vCIO is live and move it off the draft catalog first. Then make who-owner-2/-3 cover whichever branches who-owner-1 keeps. |
| 6 | major | correctness | `who-spec-2` | It names five, not eight. The line — "The table covers leadership and architecture, cloud and infrastructure, AI and data, product and delivery, and security itself." — maps to Leadership & Architecture, IT & Cloud Infrastructure, AI Development & Operations, Product & Delivery and Information Security. Three of the eight categories in builder-names.json are missing: Fractional Executive & Retainer Services, IT/IS Project Management and Standard Rate Card Services — twelve of the 33 services, including the two retainers and the project-management shapes the chapter's own `team` branch is selling. The sheet tells the owner the coverage is complete when it is not. | Either correct the justification to say five families were chosen and why the other three are dropped, or extend the line — but note that a five-item list is already at the ear limit (see the ear-test finding), so honest scoping is the better fix. |
| 7 | major | completeness | `who-price` | Wrong for two of the four branches. `who-price` fires on "people = owner, specialist or team", so an owner hears who-owner-1, -2, -3 plus who-price = four, and a specialist hears who-spec-1, -2, -3 plus who-price = four. Only team (2 + price) is three. The count is the owner's only measure of how long this new node runs, and the draft argues for the node partly on brevity. | Correct to: four (owner), four (specialist), three (team), one (none). |
| 8 | major | sourcing | `who-price` | It is not verbatim and it is not newly needed. The standing sentence is `path-price` at content/tour.json:3337 — "There's no list price here. Pricing follows scope and commitment, and the team scopes it with you." The draft's version, "No list price here; the team scopes it with you.", drops the middle clause and re-punctuates, so it is an edit of signed copy presented as a quotation. Worse, `path-price` lives in the `path` node, one or two nodes before the table on every route, so this visitor now hears the price rule twice within about a minute and then reads a third variant in the email as `sum-price`. | Either drop who-price (path-price has already said it) or say it verbatim and cut path-price. Correct the provenance note either way, and reconsider sum-price given the visitor was already told twice. |
| 9 | major | correctness | `sum-situation` | This is the only echo line in the summary gated on a chapter rather than on the answer. `arrival` is the entry chapter, so `visited: arrival` is true for everyone. js/tourtext.js fillTemplate replaces an unresolved runtime token with the empty string, so any visitor who reaches the summary without a stored `situation` gets the literal text "What brought you in: " with nothing after the colon in an email they forward. The three lines this replaces (sum-wt-trigger, sum-gc-trigger, sum-sr-trigger) were all gated on `answers`, as are the draft's own sum-program and sum-people. | Gate it the way the cut lines were gated — `answers: {situation: [<every option id>]}` — so it cannot print an empty value. Note also that the line is a hard lint error until the arrival choice exists: checkText errors with "{answer:situation}: no choice remembers situation". |
| 10 | major | consistency | `table-1` | The chapter's opening sentence contradicts the model it is written for. The draft itself defines the door under F as a shelf ("The door under F is a shelf; the twelve starts are shelf answers") — behind a door a visitor buys services and readiness packages, and only sometimes a managed program. table-2 ("The programs behind those doors are staffed from this table.") and who-none-1 ("Then a program is the thing to buy") repeat the same conflation. A visitor who has just walked Win Trust and been recommended SOC 2 Readiness is told they bought "a program". Separately, "you buy people" is a blunt way for a services firm to describe hiring its consultants, and it is the first thing the visitor hears at the table. | Rewrite the split so it holds under F: the doors hold the work, the table holds the people who do it. And find a phrase other than "you buy people" for the second half. |
| 11 | major | correctness | `close-2` | False for the "Show me everything" visitor. That route runs lobby → all-wt → all-gc → all-sr → path-all → (now) table → close, and none of those nodes carries a choice, so no `trigger-win-trust`, `trigger-gain-control`, `trigger-stay-ready` or `program` answer is ever stored. Every "Where you'd start" line is gated on a trigger answer, so that visitor's email contains no first step at all — and sum-program does not fire either. close-1 ("That's your door") is wrong for them for the same reason. The draft reasons carefully about this visitor when deciding against a door cue ("a visitor who walked two or three doors would be told the wrong one is theirs") and then leaves the identical problem in the words. | Condition close-1 and close-2 on having a door/trigger answer, with an all-doors variant, or add a summary line that gives the all-doors visitor something to start from. Apply the cue reasoning to the copy. |
| 12 | minor | ear-test | `who-spec-2` | Five paired-noun items and seven conjunctions in one breath, with no anchor between them. A listener cannot hold items three, four and five; read aloud it flattens into a list with no shape. It is the hardest line in the chapter to say and the hardest to follow. | Cut to three families, or break it into two sentences with a pause between them. |
| 13 | minor | ear-test | `table-3` | "A person works to yours" is elliptical — the noun it points back at ("your plan") is two sentences away, and "works to" is an unusual verb pairing that a listener has to unpack while the next line starts. The draft calls this "said in the plainest words I could find"; it is the least plain sentence in the node. | Say the noun: "A person works to your plan." |
| 14 | minor | repetition | `who-spec-3` | This restates table-5 ("Whichever it is, the person in the room is the person doing the work.") two lines later, and both are sourced to the same Message Stack §06 material. A specialist visitor hears the named-person claim twice in under thirty seconds, which reads as a script that lost track of itself rather than as emphasis. | Keep one. table-5 is the stronger line and covers all three branches; cut who-spec-3 or replace it with something only the specialist branch needs. |
| 15 | minor | sourcing | `sum-wt-ai` | Three summary lines the draft signs off as correct disagree with the manifest's own `doors.<id>.starts`. experience.json gives win-trust `ai` as lead ai-governance-advisory WITH initial-risk-assessment, then managed-isp — the summary drops the `with`. gain-control `reporting` and stay-ready `exam` both carry `then: [managed-rmp]`, which neither sum-gc-reporting nor sum-sr-exam mentions. The sheet asserts these lines are right without noting they are a lossy rendering of the manifest. | Say in the sheet which omissions are deliberate compression and which are drift, so the owner is not approving two different answers to the same question. |
| 16 | minor | completeness | `table-back-huey / table-back-avi` | Not flagged as a voice consequence. docs/COPY-GATE.md says a changed word needs "a re-render of that line's voice", and check-manifest.js notes voice files are named by line id (media/voice/<id>.mp3). Both a rename and a text change apply here, so two renders are orphaned and two new ones are needed per guide. The Manifest-asks section lists offer ids and a lobby surface but not this. | Add the voice re-render to the asks list, alongside the note that every other new line in the chapter also needs a first render. |
| 17 | minor | correctness | `table-who choice` | The choice id duplicates a node id in the same chapter (`table-who` is both the node the options lead to and the id of the choice that leads there) and does not match its own remember key. Nothing in check-manifest.js rejects it, but every other remembering choice in the file is either named for its key (lead, segment, program) or for its own node — none is named for a different node. It will read as a typo to the next person who edits the file. | Name the choice `people`, matching its remember key and the pattern of lead / segment / program. |
| 18 | minor | correctness | `vocabulary self-check` | Half wrong, in a self-check the owner is being asked to trust. "thirty-three" is caught because BIG contains "thirty". "eight" is in SMALL, not BIG, and SMALL only trips figures() next to a time unit, "times/fold", or "in/out of" — "eight categories" would pass the lint cleanly. The stated reason the table never counts itself aloud is not the real one. | Correct the note, or drop it. The conclusion (don't count the shelf aloud) is right; the justification is not. |

## Ask console  
`ask` · verdict **defects** · 25 findings

| # | severity | category | line | problem | fix |
|---|---|---|---|---|---|
| 1 | blocker | false-premise-cut | `ask-what-1` | Arrival does not speak the boilerplate. tour.json nodes.arrive is arrive-1..5; the second line is arrive-2 "We won't tour a building. We'll tour your situation, and the door that fits it." The Message Stack §09 boilerplate appears nowhere in the arrival chapter, and per docs/copy-sheets-tour.md:149 today's ask-what-1 is the ONLY approved-copy text line in the entire script. The chapter also contradicts itself: §2 says "Arrival's situation choice will `remember` a new key under F, and nobody has fixed its name or its option values yet" — arrival cannot simultaneously be unwritten and reliably carry the boilerplate. The replacement also drops the boilerplate's ICP qualifier ("have to prove their controls work — and have no one inside to run them") and its AI governance scope, so it is not "the same fact in different words". | Keep the approved-copy ask-what-1 verbatim until the arrival chapter has actually landed with the boilerplate in it. Add ask-what-2 and ask-what-3 after it, or hold the cut and re-raise it as a dependency on the arrival chapter. |
| 2 | blocker | false-claim | `ask-price-2` | Flatly false, and a visitor can catch it in the same session. content/experience.json door stats print: "61% were required to hold a certification to win or renew a contract.", "68% ... 93% ...", "Sponsors above $25B AUM ... 81% of the time. Below $25B, only 29% do.", "26% ...", "72% ... only 28% ...", "86.8% ... 51.1% ...", plus "1,000+ systems certified" in doors.gain-control.proof.0 which this same chapter reads aloud at ask-proof-2. The standing rule is no prices, not no numbers. | Say what is actually true: "Pricing follows scope and commitment, and no price appears on this site." |
| 3 | blocker | wrong-shelf | `ask-exam-2` | "Keeping the findings closed" is {offer:risk-register-poam} from ask-exam-1. offers.risk-register-poam = "Manage Risk Register & POA&M", category "Risk Assessment & Risk Management" (confirmed in builder-names.json, draft: false) — a Win Trust shelf under Candidate F, not Stay Ready. Both offers this answer names live on Win Trust, so the line sends the visitor to the wrong shelf and the retained `goto: sr` compounds it: the visitor is walked into Stay Ready and neither named service is there. | Either name a real Stay Ready service for the ongoing half (a Managed GRC Programs offer such as {offer:managed-rmp}), or say both sit behind Win Trust and move the goto to `wt`. Do not describe risk-register-poam as Stay Ready. |
| 4 | major | false-provenance | `ask-price-1` | "No list price here; the team scopes it with you." is not the settled sentence and appears nowhere in the repo. The settled line is tour.json path-price (docs/copy-sheets-tour.md:81 and :851): "There's no list price here. Pricing follows scope and commitment, and the team scopes it with you." The draft has compressed it, swapped a full stop for a semicolon, and then asserted "word for word" twice — once here and again for ask-staffing-3. The follow-on claim that ask-price-2 removes duplication also fails, since the draft's ask-price-1 + ask-price-2 together just re-split the settled sentence. | Use the settled sentence verbatim in both places, or drop the "word for word" claim and put the rewording in front of the owner as a deliberate change. |
| 5 | major | misquote | `ask-pe-2` | The manifest fields quoted as justification for cutting ask-pe-2/ask-pe-3 do not say that. doors.gain-control.program.build reads "One control set applied across every operating company, isolated per entity." and .operate reads "Quarterly board- and investment-committee-ready reporting, with the fund seeing one view." Neither contains the phrase, and neither names a buyer type. The stated reason for the cut is therefore fabricated — and .operate is status approved-copy, so the owner is being asked to drop approved copy on a misquote. | Re-read doors.gain-control.program.build/.operate and either restate the real reason for the cut or keep the refs. |
| 6 | major | missing-dependency | `ask-packages-4` | Every new shelf sentence in the chapter contradicts the live manifest, and no Open flag says so. Today doors.win-trust.packages = [soc-2-readiness, iso-27001-readiness, iso-27701-readiness] (no PCI-DSS); doors.stay-ready.packages = [ir-fast-start, privacy-leadership-launch, mxdr-complete, pci-dss-readiness]; doors.gain-control.packages = []; doors.gain-control.programs includes rmp. So if Ask ships before the doors chapter, the guide says PCI-DSS Readiness is behind Win Trust while the Stay Ready room lists it, says Privacy Leadership Launch is behind Gain Control while Stay Ready lists it, and says Managed Risk Management Program is behind Stay Ready while Gain Control lists it. Same for serviceFamilies: gain-control's families are Risk Assessment, Managed GRC Programs, Vendor & Third-Party, Information Security Program & Governance — neither Privacy nor Security Engineering & Architecture, which the chapter says Gain Control holds. | Add an Open flag naming doors.*.packages, doors.*.programs and doors.*.serviceFamilies as hard prerequisites, and state that Ask cannot ship ahead of the doors chapter. |
| 7 | major | held-facts | `held-facts-table` | The brief is that the held facts must not appear at all. The chapter prints them repeatedly in visitor-invisible commentary that nevertheless goes to the owner: "under 6 months", "as fast as 90 days", "the 15-minute notification figure", "Whitedog", "as low as 50% of the standard rate", "up to 60% less audit prep", "the maturity scorecard" — a whole table plus a per-node "Held-fact check" line. Whitedog in particular is a named third party with no entry in vocabulary.allowedCustomers (which holds only "Transit Technologies" and "a large North American bank"). | Replace the table with a bare list of the question ids that were checked for held facts, naming none of them, and drop the named partner entirely. |
| 8 | major | retired-framing | `ask-frameworks-1` | Open flag 1 treats "the three segments" as a citation problem only ("it is a citation, not a claim"). It is not. The bodies of ask-frameworks-1 and ask-frameworks-2 reconstruct the retired audience labels as spoken labels: "For a bank, a credit union or a lender" is doors.stay-ready.icp "Regulated operators", and "For a software company selling into enterprise" is doors.win-trust.icp "SaaS & AI vendors". The chapter marks frameworks-1 as unchanged and frameworks-2 as merely trimmed, so the framing F retires survives untouched in the two answers most likely to be asked. | Re-sort both lines by the obligation rather than by the buyer — "If an examiner is asking..." / "If an enterprise buyer is asking..." — and widen Open flag 1 to cover the body text, not just the printed citation. |
| 9 | major | unlistenable | `ask-vendor-2` | programs.vcp.name and offers.managed-vcp.name are the identical string "Managed Vendor Compliance Program (VCP)". Filled, the answer reads: line 1 "The Managed Vendor Compliance Program (VCP) holds vendor practices...", line 2 "On the service list it's the Managed Vendor Compliance Program (VCP), behind Gain Control." The listener hears a sentence that says a thing is itself. ask-rmp-2 has exactly the same collision (programs.rmp.name == offers.managed-rmp.name == "Managed Risk Management Program", and the question itself is "What is the {program:rmp}?"). The rewrite from "It's quoted as" fixed the verb but left the tautology. | Drop the redundant naming and keep only the new information: "...proactively. It sits behind {door:gain-control.title}." Same for ask-rmp-2. |
| 10 | major | undefined-reference | `ask-packages-3` | Spoken, that is five long proper nouns in one breath — "ISO 27701 Privacy Readiness, PCI-DSS Readiness, Privacy Leadership Launch, Incident Response Fast Start and MXDR Complete Protection" — nothing a listener can hold. The next line then says "The four readiness packages sit behind Win Trust", but the visitor was never told which four: they heard two in line 2 and five in line 3, and three of those five are readiness packages. "The four readiness packages" refers to a grouping the answer never makes. | Split into two shorter lines grouped by shelf, and name the four rather than counting them: "...ISO 27701 Privacy Readiness and PCI-DSS Readiness, also behind Win Trust. Then Privacy Leadership Launch behind Gain Control, and Incident Response Fast Start and MXDR Complete Protection behind Stay Ready." |
| 11 | major | unintroduced-term | `ask-staffing-1` | "The round table" already means something else in this script, and it collides. In tour.json the round table is where the non-lead guide waits: handoff-avi-1 "I'll be at the round table when you're done." and handoff-huey-1 "{guide:huey} is all yours. I'll see you at the round table." Four new lines (ask-what-3, ask-hire-3, ask-staffing-1, ask-ai-3) now use it as the name of a hiring shelf, with no line anywhere introducing that meaning. Ask can be opened from `arrive`, so a visitor can hear "a person at the round table" before ever seeing the lobby, having just been told the other guide is sitting there. | Either add one introducing clause the first time it is used ("the round table in the lobby, where 3HUE's people are"), or flag to the owner that the two meanings need reconciling in the arrival and close chapters before these four lines are approved. |
| 12 | major | self-contradiction | `ask-exam-3` | False, and contradicted three blocks later in the same document. ask-exam-3 and ask-proof-3 both ref doors.stay-ready.proof.0, whose status is approved-copy — and the ask-proof Sources block in this very chapter prints "`ask-proof-3` → \"3HUE approved 100-word boilerplate\" · `approved-copy`". If the intent was "the only text line", that is also false, because both are ref lines and neither is text. | Delete the claim or restate it correctly: two Ask lines in this chapter carry approved-copy, both through the same ref. |
| 13 | major | wrong-diff | `chips-table` | Three rows of the chips table state changes that are not the changes. Today nodes.wt-receive.ask = ['packages','auditor','timeline','pricing'] — there is no `frameworks` chip to swap out and `timeline` is already there; the row is a pure reorder. Today nodes.wt-proof.ask = ['proof','auditor','guarantee','theater'] — `theater` is already there, so the real swap is auditor → different, not auditor → theater. Today nodes.path-all.ask = ['pricing','quote','prepare','after'], but the row is labelled "unchanged" while the draft lists `where-to-start`, `prepare`, `after`, `quote` — `pricing` is silently dropped. Separately, the `close` row's rationale says "`proof` and `different` move earlier, where doubt actually lands", but no row in the table adds `proof` anywhere it was not already, so `proof` is simply deleted from the tour's last node. | Re-derive every row from nodes.<id>.ask in content/tour.json and rewrite the three notes; then either place `proof` on an earlier node or say plainly that it is being dropped. |
| 14 | minor | arithmetic | `open-flag-7` | The two figures are counted on different bases, so the copy sheet's "At a glance" would be wrong either way. docs/copy-sheets-tour.md:147 reads "Ask has 35 questions, answered in 69 spoken lines" — 69 counts the ask.intro line; the 35 questions hold 68 lines between them. The chapter adds 16 new line ids, so on the copy sheet's basis the new number is 85, and on a questions-only basis it is 68 → 84. | State it as 37 questions / 85 spoken lines (was 35 / 69), matching how the copy sheet counts. |
| 15 | minor | ear-test | `ask-deploy-2` | 29 words filled, the longest new line in the chapter, and it carries the two longest product names on the catalog back to back: "AiVRIC CloudSignals GRCOps & CSPM Onboarding puts your program on AiVRIC. M365 Modern GRC System Deployment & Maintenance builds it in the tenant you already own." Under guide.voice.say that is "av-RICK CloudSignals GRCOps and CSPM Onboarding ... av-RICK ... Microsoft three sixty-five Modern GRC System Deployment and Maintenance" — AiVRIC said twice in one breath, six initialisms in 29 words. | Split into two lines, one per route, and drop the second "AiVRIC": "{offer:aivric-cloudsignals-grcops-cspm-onboarding} puts your program on the platform." / "{offer:m365-modern-grc-system-deployment-maintenance} builds it in the tenant you already own." |
| 16 | minor | ear-test | `ask-grc-2` | The name already ends "Deployment & Maintenance", so the tail repeats it word for word: "...M365 Modern GRC System Deployment & Maintenance, deployed and then maintained." And line 1 has just said "deploy it in your Microsoft 365 tenant", which the voice map turns into the same "Microsoft three sixty-five" the name then repeats. | Cut the tail: "That's kiosk work: {offer:m365-modern-grc-system-deployment-maintenance}." |
| 17 | minor | wrong-shelf | `open-flag-6` | Pointing `vciso` at the round table would be wrong. The only offer that answer names is {offer:ciso-support} = "CISO Support", category "Information Security Program & Governance" — a Win Trust shelf under Candidate F. Only the unnamed "fractional CISO" half is a round-table item (Virtual CISO — Fractional, Fractional Executive & Retainer Services, held). The flag also cites ask-staffing-3 as pointing at the round table; ask-staffing-3 is "No list price here; the team scopes it with you." — it is ask-staffing-1 that names it. | Drop `vciso` from that list, or note that its goto is genuinely split between Win Trust and the round table. Correct the ask-staffing-3 reference to ask-staffing-1. |
| 18 | minor | wrong-node | `reasoning-2` | The situation/door choice is not on arrival. In tour.json, nodes.arrive.choice is id "lead", remember "lead" (which guide leads); the segment choice is nodes.lobby.choice, id "segment", remember "segment", with the three {door:*.icp} labels. Since this misidentification is the whole reason given for "Ask carries no `when` conditions", the rationale rests on the wrong node. | Rewrite as "The lobby's `segment` choice is being replaced under F..." — the conclusion still holds, the reason needs correcting. |
| 19 | minor | false-provenance | `ask-quote-1` | "Not on this site. The team builds it with you; your summary names your first step." appears nowhere in the repo — no settled version exists to match. And it is not identical to anything: nodes.close.lines close-2 reads "Your summary names that first step, so the conversation with the team starts there." The stated reason for the rewrite was that the old line "repeated close-2 almost exactly"; the new line still repeats close-2's clause, so the duplication the rewrite was for is still there. | Drop the "settled words" and "identical wording" claims, or quote the real settled source. If the goal is to stop repeating close-2, cut the summary clause entirely: "Not on this site. The team builds it with you." |
| 20 | minor | dead-end-goto | `ask-staffing` | nodes.close exists and its chapter title is "The round table", but the node itself says nothing about hiring people: close-1 "That's your door, and where you'd start.", close-2 the summary line, close-3 the four options (talk, summary, walk another door, ask). A visitor who asks "Can we hire people from you instead of a program?" and presses Take me there lands on the tour's ending with no round-table content and no hiring option in the choice. The same problem sits behind Open flag 6 without being named. | Hold `staffing.goto` until the close chapter actually carries round-table content, or say in the flag that the target is a chapter title only, not a room with anything on it yet. |
| 21 | minor | weak-source | `ask-what-3` | Ask prints the source to the visitor. The single most load-bearing orientation sentence in the chapter — how 3HUE is bought — would print "Tour script, linking line", which reads as "we made this up". The same applies to ask-packages-4 and ask-pe-2, both of which state shelf placements that are derived from builder-names.json categories and so have a real citation available. | Cite "3HUE service catalog, September 2026" on ask-what-3, ask-packages-4 and ask-pe-2, since the placements come from the catalog capture, not from the tour script. |
| 22 | minor | ear-test | `ask-staffing-2` | The "run from X to Y" frame closes at "AI and data people", then three more items are bolted on after it, so the sentence has no grammatical landing. Read aloud it sounds like the speaker lost the thread. "AI and data people" and "product and delivery" also stop being roles and become department names mid-list. | Drop the range frame: "Security architects, cloud engineers, AI and data specialists, product and delivery, project managers, and fractional executives." |
| 23 | minor | id-reuse | `ask-what-1` | Refilling ids hides the diff on exactly the lines that most need review. ask-what-1 keeps its id while changing from The Message Stack §09 boilerplate at status approved-copy to new text at status adapted; ask-start-1, ask-pe-2, ask-pe-3 and ask-exam-2 all change from ref lines to text lines under their old ids; and ask-price-2's current text moves to ask-price-3 while ask-price-2 becomes something new. A reviewer diffing by line id sees stable ids and will not see that the script's only approved-copy text line is gone. | Give genuinely new copy new ids and list the retired ones, at minimum for ask-what-1 where the status changes. |
| 24 | minor | self-referential-chip | `chips-path` | The where-to-start question carries `goto: path`. Putting its chip on the `path` node gives the visitor a Take me there button that walks them to the node they are already standing on. | Leave `quote` (or use `timeline`) on `path`, and keep `where-to-start` on nodes the visitor can actually travel from. |
| 25 | minor | placeholder | `chips-table` | Two rows of the chips table are placeholders for nodes that do not exist (the second is "*(a kiosk node, if the doors chapter adds one)*"), so the table cannot be applied as written. The chapter is also explicit that three questions are "still not written" (the virtual CIO comparison, the Get-Well methodology, the AI Snapshot), which is fine as a hold but is not reflected in the 37-question total anywhere. | Move both rows into Open flags as conditional instructions rather than table rows, and state in the scope line whether the three held questions are inside or outside the 37. |

